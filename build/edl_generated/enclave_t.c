/*
 *  This file is auto generated by oeedger8r. DO NOT EDIT.
 */
#include "enclave_t.h"

#include <openenclave/edger8r/enclave.h>

OE_EXTERNC_BEGIN

/* Set to false to bypass secure unserializing ocall return values */
OE_WEAK bool oe_edger8r_secure_unserialize = true;

/**** Trusted function IDs ****/
enum
{
    enclave_fcn_id_initialize_enclave_ml_context = 0,
    enclave_fcn_id_enclave_infer = 1,
    enclave_fcn_id_terminate_enclave_ml_context = 2,
    enclave_fcn_id_oe_get_sgx_report_ecall = 3,
    enclave_fcn_id_oe_get_report_v2_ecall = 4,
    enclave_fcn_id_oe_verify_local_report_ecall = 5,
    enclave_fcn_id_oe_sgx_init_context_switchless_ecall = 6,
    enclave_fcn_id_oe_sgx_switchless_enclave_worker_thread_ecall = 7,
    enclave_fcn_id_oe_log_init_ecall = 8,
    enclave_fcn_id_trusted_call_id_max = OE_ENUM_MAX
};

/**** ECALL marshalling structs. ****/
typedef struct _initialize_enclave_ml_context_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    unsigned char* model_data;
    size_t model_size;
    uint64_t* enclave_session_handle;
} initialize_enclave_ml_context_args_t;

typedef struct _enclave_infer_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint64_t enclave_session_handle;
    int64_t* input_data;
    size_t input_data_byte_size;
    float* output_buffer;
    size_t output_buffer_byte_size;
    size_t* actual_output_size_bytes_out;
} enclave_infer_args_t;

typedef struct _terminate_enclave_ml_context_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint64_t enclave_session_handle;
} terminate_enclave_ml_context_args_t;

typedef struct _oe_get_sgx_report_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    void* opt_params;
    size_t opt_params_size;
    sgx_report_t* report;
} oe_get_sgx_report_ecall_args_t;

typedef struct _oe_get_report_v2_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint32_t flags;
    void* opt_params;
    size_t opt_params_size;
    oe_report_buffer_t* report;
} oe_get_report_v2_ecall_args_t;

typedef struct _oe_verify_local_report_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint8_t* report;
    size_t report_size;
    oe_report_t* parsed_report;
} oe_verify_local_report_ecall_args_t;

typedef struct _oe_sgx_init_context_switchless_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_host_worker_context_t* host_worker_contexts;
    uint64_t num_host_workers;
} oe_sgx_init_context_switchless_ecall_args_t;

typedef struct _oe_sgx_switchless_enclave_worker_thread_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_enclave_worker_context_t* context;
} oe_sgx_switchless_enclave_worker_thread_ecall_args_t;

typedef struct _oe_log_init_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    char* enclave_path;
    size_t enclave_path_len;
    uint32_t log_level;
} oe_log_init_ecall_args_t;

/**** ECALL functions. ****/

static void ecall_initialize_enclave_ml_context(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    initialize_enclave_ml_context_args_t* _pargs_in = (initialize_enclave_ml_context_args_t*)input_buffer;
    initialize_enclave_ml_context_args_t* _pargs_out = (initialize_enclave_ml_context_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (_pargs_in->model_data)
        OE_SET_IN_POINTER(model_data, 1, _pargs_in->model_size, unsigned char*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->enclave_session_handle)
        OE_SET_OUT_POINTER(enclave_session_handle, 1, sizeof(uint64_t), uint64_t*);

    /* Check that in/in-out strings are null terminated. */
    /* There were no in nor in-out string parameters. */

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    _pargs_out->oe_retval = initialize_enclave_ml_context(
        (const unsigned char*)_pargs_in->model_data,
        _pargs_in->model_size,
        _pargs_in->enclave_session_handle);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*_pargs_out) &&
        oe_is_within_enclave(_pargs_out, output_buffer_size))
        _pargs_out->oe_result = _result;
}

static void ecall_enclave_infer(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    enclave_infer_args_t* _pargs_in = (enclave_infer_args_t*)input_buffer;
    enclave_infer_args_t* _pargs_out = (enclave_infer_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (_pargs_in->input_data)
        OE_SET_IN_POINTER(input_data, _pargs_in->input_data_byte_size, sizeof(int64_t), int64_t*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->output_buffer)
        OE_SET_OUT_POINTER(output_buffer, _pargs_in->output_buffer_byte_size, sizeof(float), float*);
    if (_pargs_in->actual_output_size_bytes_out)
        OE_SET_OUT_POINTER(actual_output_size_bytes_out, 1, sizeof(size_t), size_t*);

    /* Check that in/in-out strings are null terminated. */
    /* There were no in nor in-out string parameters. */

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    _pargs_out->oe_retval = enclave_infer(
        _pargs_in->enclave_session_handle,
        (const int64_t*)_pargs_in->input_data,
        _pargs_in->input_data_byte_size,
        _pargs_in->output_buffer,
        _pargs_in->output_buffer_byte_size,
        _pargs_in->actual_output_size_bytes_out);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*_pargs_out) &&
        oe_is_within_enclave(_pargs_out, output_buffer_size))
        _pargs_out->oe_result = _result;
}

static void ecall_terminate_enclave_ml_context(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    terminate_enclave_ml_context_args_t* _pargs_in = (terminate_enclave_ml_context_args_t*)input_buffer;
    terminate_enclave_ml_context_args_t* _pargs_out = (terminate_enclave_ml_context_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Check that in/in-out strings are null terminated. */
    /* There were no in nor in-out string parameters. */

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    _pargs_out->oe_retval = terminate_enclave_ml_context(
        _pargs_in->enclave_session_handle);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*_pargs_out) &&
        oe_is_within_enclave(_pargs_out, output_buffer_size))
        _pargs_out->oe_result = _result;
}

static void ecall_oe_get_sgx_report_ecall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    oe_get_sgx_report_ecall_args_t* _pargs_in = (oe_get_sgx_report_ecall_args_t*)input_buffer;
    oe_get_sgx_report_ecall_args_t* _pargs_out = (oe_get_sgx_report_ecall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (_pargs_in->opt_params)
        OE_SET_IN_POINTER(opt_params, 1, _pargs_in->opt_params_size, void*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->report)
        OE_SET_OUT_POINTER(report, 1, sizeof(sgx_report_t), sgx_report_t*);

    /* Check that in/in-out strings are null terminated. */
    /* There were no in nor in-out string parameters. */

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    _pargs_out->oe_retval = oe_get_sgx_report_ecall(
        (const void*)_pargs_in->opt_params,
        _pargs_in->opt_params_size,
        _pargs_in->report);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*_pargs_out) &&
        oe_is_within_enclave(_pargs_out, output_buffer_size))
        _pargs_out->oe_result = _result;
}

static void ecall_oe_get_report_v2_ecall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    oe_get_report_v2_ecall_args_t* _pargs_in = (oe_get_report_v2_ecall_args_t*)input_buffer;
    oe_get_report_v2_ecall_args_t* _pargs_out = (oe_get_report_v2_ecall_args_t*)output_buffer;

    uint8_t* _deepcopy_out_buffer = NULL;
    size_t _deepcopy_out_buffer_offset = 0;
    size_t _deepcopy_out_buffer_size = 0;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (_pargs_in->opt_params)
        OE_SET_IN_POINTER(opt_params, 1, _pargs_in->opt_params_size, void*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->report)
        OE_SET_OUT_POINTER(report, 1, sizeof(oe_report_buffer_t), oe_report_buffer_t*);

    /* Check that in/in-out strings are null terminated. */
    /* There were no in nor in-out string parameters. */

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    _pargs_out->oe_retval = oe_get_report_v2_ecall(
        _pargs_in->flags,
        (const void*)_pargs_in->opt_params,
        _pargs_in->opt_params_size,
        _pargs_in->report);

    /* Compute the size for the deep-copy out buffer. */
    if (_pargs_in->report && _pargs_in->report->buffer)
        OE_ADD_ARG_SIZE(_deepcopy_out_buffer_size, 1, _pargs_in->report->size);

    if (_deepcopy_out_buffer_size)
    {
        _deepcopy_out_buffer = (uint8_t*) oe_malloc(_deepcopy_out_buffer_size);
        if (!_deepcopy_out_buffer)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }
    }

    /* Serialize the deep-copied content into the buffer. */
    if (_pargs_in->report && _pargs_in->report->buffer)
        OE_WRITE_DEEPCOPY_OUT_PARAM(_pargs_in->report->buffer, 1, _pargs_in->report->size);

    if (_deepcopy_out_buffer_offset != _deepcopy_out_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Set the _deepcopy_out_buffer and _deepcopy_out_buffer as part of _pargs_out. */
    _pargs_out->deepcopy_out_buffer = _deepcopy_out_buffer;
    _pargs_out->deepcopy_out_buffer_size = _deepcopy_out_buffer_size;

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    /* Free _pargs_out->deepcopy_out_buffer on failure. */
    if (_result != OE_OK)
    {
        oe_free(_pargs_out->deepcopy_out_buffer);
        _pargs_out->deepcopy_out_buffer = NULL;
        _pargs_out->deepcopy_out_buffer_size = 0;
    }

    /* Free nested buffers allocated by the user function. */
    if (_pargs_in->report)
    {
        for (size_t _i_1 = 0; _i_1 < 1; _i_1++)
        {
            free(_pargs_in->report[_i_1].buffer);
        }
    }

    if (output_buffer_size >= sizeof(*_pargs_out) &&
        oe_is_within_enclave(_pargs_out, output_buffer_size))
        _pargs_out->oe_result = _result;
}

static void ecall_oe_verify_local_report_ecall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    oe_verify_local_report_ecall_args_t* _pargs_in = (oe_verify_local_report_ecall_args_t*)input_buffer;
    oe_verify_local_report_ecall_args_t* _pargs_out = (oe_verify_local_report_ecall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (_pargs_in->report)
        OE_SET_IN_POINTER(report, 1, _pargs_in->report_size, uint8_t*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->parsed_report)
        OE_SET_OUT_POINTER(parsed_report, 1, sizeof(oe_report_t), oe_report_t*);

    /* Check that in/in-out strings are null terminated. */
    /* There were no in nor in-out string parameters. */

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    _pargs_out->oe_retval = oe_verify_local_report_ecall(
        (const uint8_t*)_pargs_in->report,
        _pargs_in->report_size,
        _pargs_in->parsed_report);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*_pargs_out) &&
        oe_is_within_enclave(_pargs_out, output_buffer_size))
        _pargs_out->oe_result = _result;
}

static void ecall_oe_sgx_init_context_switchless_ecall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    oe_sgx_init_context_switchless_ecall_args_t* _pargs_in = (oe_sgx_init_context_switchless_ecall_args_t*)input_buffer;
    oe_sgx_init_context_switchless_ecall_args_t* _pargs_out = (oe_sgx_init_context_switchless_ecall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Check that in/in-out strings are null terminated. */
    /* There were no in nor in-out string parameters. */

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    _pargs_out->oe_retval = oe_sgx_init_context_switchless_ecall(
        _pargs_in->host_worker_contexts,
        _pargs_in->num_host_workers);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*_pargs_out) &&
        oe_is_within_enclave(_pargs_out, output_buffer_size))
        _pargs_out->oe_result = _result;
}

static void ecall_oe_sgx_switchless_enclave_worker_thread_ecall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    oe_sgx_switchless_enclave_worker_thread_ecall_args_t* _pargs_in = (oe_sgx_switchless_enclave_worker_thread_ecall_args_t*)input_buffer;
    oe_sgx_switchless_enclave_worker_thread_ecall_args_t* _pargs_out = (oe_sgx_switchless_enclave_worker_thread_ecall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Check that in/in-out strings are null terminated. */
    /* There were no in nor in-out string parameters. */

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    oe_sgx_switchless_enclave_worker_thread_ecall(
        _pargs_in->context);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*_pargs_out) &&
        oe_is_within_enclave(_pargs_out, output_buffer_size))
        _pargs_out->oe_result = _result;
}

static void ecall_oe_log_init_ecall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    oe_log_init_ecall_args_t* _pargs_in = (oe_log_init_ecall_args_t*)input_buffer;
    oe_log_init_ecall_args_t* _pargs_out = (oe_log_init_ecall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (_pargs_in->enclave_path)
        OE_SET_IN_POINTER(enclave_path, _pargs_in->enclave_path_len, sizeof(char), char*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Check that in/in-out strings are null terminated. */
    OE_CHECK_NULL_TERMINATOR(_pargs_in->enclave_path, _pargs_in->enclave_path_len);

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    oe_log_init_ecall(
        (const char*)_pargs_in->enclave_path,
        _pargs_in->log_level);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*_pargs_out) &&
        oe_is_within_enclave(_pargs_out, output_buffer_size))
        _pargs_out->oe_result = _result;
}

/**** ECALL function table. ****/

oe_ecall_func_t oe_ecalls_table[] = {
    (oe_ecall_func_t) ecall_initialize_enclave_ml_context,
    (oe_ecall_func_t) ecall_enclave_infer,
    (oe_ecall_func_t) ecall_terminate_enclave_ml_context,
    (oe_ecall_func_t) ecall_oe_get_sgx_report_ecall,
    (oe_ecall_func_t) ecall_oe_get_report_v2_ecall,
    (oe_ecall_func_t) ecall_oe_verify_local_report_ecall,
    (oe_ecall_func_t) ecall_oe_sgx_init_context_switchless_ecall,
    (oe_ecall_func_t) ecall_oe_sgx_switchless_enclave_worker_thread_ecall,
    (oe_ecall_func_t) ecall_oe_log_init_ecall
};

size_t oe_ecalls_table_size = OE_COUNTOF(oe_ecalls_table);

/**** Untrusted function IDs. ****/
enum
{
    enclave_fcn_id_ocall_ggml_load_model = 0,
    enclave_fcn_id_ocall_ggml_run_inference = 1,
    enclave_fcn_id_ocall_ggml_release_session = 2,
    enclave_fcn_id_oe_get_supported_attester_format_ids_ocall = 3,
    enclave_fcn_id_oe_get_qetarget_info_ocall = 4,
    enclave_fcn_id_oe_get_quote_ocall = 5,
    enclave_fcn_id_oe_get_quote_verification_collateral_ocall = 6,
    enclave_fcn_id_oe_get_quote_verification_collateral_with_baseline_ocall = 7,
    enclave_fcn_id_oe_verify_quote_ocall = 8,
    enclave_fcn_id_oe_sgx_get_cpuid_table_ocall = 9,
    enclave_fcn_id_oe_sgx_backtrace_symbols_ocall = 10,
    enclave_fcn_id_oe_sgx_log_backtrace_ocall = 11,
    enclave_fcn_id_oe_sgx_thread_wake_wait_ocall = 12,
    enclave_fcn_id_oe_sgx_wake_switchless_worker_ocall = 13,
    enclave_fcn_id_oe_sgx_sleep_switchless_worker_ocall = 14,
    enclave_fcn_id_oe_get_tdx_quote_verification_collateral_ocall = 15,
    enclave_fcn_id_oe_verify_tdx_quote_ocall = 16,
    enclave_fcn_id_oe_log_is_supported_ocall = 17,
    enclave_fcn_id_oe_log_ocall = 18,
    enclave_fcn_id_oe_write_ocall = 19,
    enclave_fcn_id_untrusted_call_max = OE_ENUM_MAX
};

/**** OCALL marshalling structs. ****/
typedef struct _ocall_ggml_load_model_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_result_t* ocall_host_ret;
    oe_result_t* host_return_value;
    uint64_t* host_session_handle;
    unsigned char* model_data;
    size_t model_data_len;
} ocall_ggml_load_model_args_t;

typedef struct _ocall_ggml_run_inference_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_result_t* ocall_host_ret;
    oe_result_t* host_return_value;
    uint64_t host_session_handle;
    void* input_data;
    size_t input_len;
    void* output_data;
    size_t output_buf_len;
    size_t* actual_output_len;
} ocall_ggml_run_inference_args_t;

typedef struct _ocall_ggml_release_session_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_result_t* ocall_host_ret;
    oe_result_t* host_return_value;
    uint64_t host_session_handle;
} ocall_ggml_release_session_args_t;

typedef struct _oe_get_supported_attester_format_ids_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    format_ids_t* format_ids;
} oe_get_supported_attester_format_ids_ocall_args_t;

typedef struct _oe_get_qetarget_info_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_uuid_t* format_id;
    void* opt_params;
    size_t opt_params_size;
    sgx_target_info_t* target_info;
} oe_get_qetarget_info_ocall_args_t;

typedef struct _oe_get_quote_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_uuid_t* format_id;
    void* opt_params;
    size_t opt_params_size;
    sgx_report_t* sgx_report;
    void* quote;
    size_t quote_size;
    size_t* quote_size_out;
} oe_get_quote_ocall_args_t;

typedef struct _oe_get_quote_verification_collateral_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint8_t* fmspc;
    uint8_t collateral_provider;
    void* tcb_info;
    size_t tcb_info_size;
    size_t* tcb_info_size_out;
    void* tcb_info_issuer_chain;
    size_t tcb_info_issuer_chain_size;
    size_t* tcb_info_issuer_chain_size_out;
    void* pck_crl;
    size_t pck_crl_size;
    size_t* pck_crl_size_out;
    void* root_ca_crl;
    size_t root_ca_crl_size;
    size_t* root_ca_crl_size_out;
    void* pck_crl_issuer_chain;
    size_t pck_crl_issuer_chain_size;
    size_t* pck_crl_issuer_chain_size_out;
    void* qe_identity;
    size_t qe_identity_size;
    size_t* qe_identity_size_out;
    void* qe_identity_issuer_chain;
    size_t qe_identity_issuer_chain_size;
    size_t* qe_identity_issuer_chain_size_out;
} oe_get_quote_verification_collateral_ocall_args_t;

typedef struct _oe_get_quote_verification_collateral_with_baseline_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint8_t* fmspc;
    uint8_t collateral_provider;
    void* baseline;
    size_t baseline_size;
    void* tcb_info;
    size_t tcb_info_size;
    size_t* tcb_info_size_out;
    void* tcb_info_issuer_chain;
    size_t tcb_info_issuer_chain_size;
    size_t* tcb_info_issuer_chain_size_out;
    void* pck_crl;
    size_t pck_crl_size;
    size_t* pck_crl_size_out;
    void* root_ca_crl;
    size_t root_ca_crl_size;
    size_t* root_ca_crl_size_out;
    void* pck_crl_issuer_chain;
    size_t pck_crl_issuer_chain_size;
    size_t* pck_crl_issuer_chain_size_out;
    void* qe_identity;
    size_t qe_identity_size;
    size_t* qe_identity_size_out;
    void* qe_identity_issuer_chain;
    size_t qe_identity_issuer_chain_size;
    size_t* qe_identity_issuer_chain_size_out;
} oe_get_quote_verification_collateral_with_baseline_ocall_args_t;

typedef struct _oe_verify_quote_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_uuid_t* format_id;
    void* opt_params;
    size_t opt_params_size;
    void* p_quote;
    uint32_t quote_size;
    time_t expiration_check_date;
    uint32_t* p_collateral_expiration_status;
    uint32_t* p_quote_verification_result;
    void* p_qve_report_info;
    uint32_t qve_report_info_size;
    void* p_supplemental_data;
    uint32_t supplemental_data_size;
    uint32_t* p_supplemental_data_size_out;
    uint32_t collateral_version;
    void* p_tcb_info;
    uint32_t tcb_info_size;
    void* p_tcb_info_issuer_chain;
    uint32_t tcb_info_issuer_chain_size;
    void* p_pck_crl;
    uint32_t pck_crl_size;
    void* p_root_ca_crl;
    uint32_t root_ca_crl_size;
    void* p_pck_crl_issuer_chain;
    uint32_t pck_crl_issuer_chain_size;
    void* p_qe_identity;
    uint32_t qe_identity_size;
    void* p_qe_identity_issuer_chain;
    uint32_t qe_identity_issuer_chain_size;
} oe_verify_quote_ocall_args_t;

typedef struct _oe_sgx_get_cpuid_table_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    void* cpuid_table_buffer;
    size_t cpuid_table_buffer_size;
} oe_sgx_get_cpuid_table_ocall_args_t;

typedef struct _oe_sgx_backtrace_symbols_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_enclave_t* oe_enclave;
    uint64_t* buffer;
    size_t size;
    void* symbols_buffer;
    size_t symbols_buffer_size;
    size_t* symbols_buffer_size_out;
} oe_sgx_backtrace_symbols_ocall_args_t;

typedef struct _oe_sgx_log_backtrace_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_enclave_t* oe_enclave;
    oe_log_level_t level;
    uint64_t* buffer;
    size_t size;
} oe_sgx_log_backtrace_ocall_args_t;

typedef struct _oe_sgx_thread_wake_wait_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_enclave_t* oe_enclave;
    uint64_t waiter_tcs;
    uint64_t self_tcs;
} oe_sgx_thread_wake_wait_ocall_args_t;

typedef struct _oe_sgx_wake_switchless_worker_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_host_worker_context_t* context;
} oe_sgx_wake_switchless_worker_ocall_args_t;

typedef struct _oe_sgx_sleep_switchless_worker_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_enclave_worker_context_t* context;
} oe_sgx_sleep_switchless_worker_ocall_args_t;

typedef struct _oe_get_tdx_quote_verification_collateral_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    void* p_quote;
    uint32_t quote_size;
    tdx_quote_collateral_t* collateral;
} oe_get_tdx_quote_verification_collateral_ocall_args_t;

typedef struct _oe_verify_tdx_quote_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_uuid_t* format_id;
    void* opt_params;
    size_t opt_params_size;
    void* p_quote;
    uint32_t quote_size;
    void* p_endorsements;
    uint32_t endorsements_size;
    time_t expiration_check_date;
    uint32_t* p_collateral_expiration_status;
    uint32_t* p_quote_verification_result;
    void* p_qve_report_info;
    uint32_t qve_report_info_size;
    void* p_supplemental_data;
    uint32_t supplemental_data_size;
    uint32_t* p_supplemental_data_size_out;
} oe_verify_tdx_quote_ocall_args_t;

typedef struct _oe_log_is_supported_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
} oe_log_is_supported_ocall_args_t;

typedef struct _oe_log_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    uint32_t log_level;
    char* message;
    size_t message_len;
} oe_log_ocall_args_t;

typedef struct _oe_write_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    int device;
    char* str;
    size_t str_len;
    size_t maxlen;
} oe_write_ocall_args_t;

/**** OCALL function wrappers. ****/

oe_result_t ocall_ggml_load_model(
    oe_result_t* _retval,
    oe_result_t* ocall_host_ret,
    oe_result_t* host_return_value,
    uint64_t* host_session_handle,
    const unsigned char* model_data,
    size_t model_data_len)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    ocall_ggml_load_model_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.ocall_host_ret = (oe_result_t*)ocall_host_ret;
    _args.host_return_value = (oe_result_t*)host_return_value;
    _args.host_session_handle = (uint64_t*)host_session_handle;
    _args.model_data = (unsigned char*)model_data;
    _args.model_data_len = model_data_len;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(ocall_ggml_load_model_args_t));
    if (model_data)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.model_data_len);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(ocall_ggml_load_model_args_t));
    if (ocall_host_ret)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(oe_result_t));
    if (host_return_value)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(oe_result_t));
    if (host_session_handle)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(uint64_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (ocall_ggml_load_model_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (model_data)
        OE_WRITE_IN_PARAM_WITH_BARRIER(model_data, 1, _args.model_data_len, unsigned char*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_ocall_ggml_load_model,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (ocall_ggml_load_model_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(ocall_host_ret, 1, sizeof(oe_result_t));
    OE_READ_OUT_PARAM(host_return_value, 1, sizeof(oe_result_t));
    OE_READ_OUT_PARAM(host_session_handle, 1, sizeof(uint64_t));

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t ocall_ggml_run_inference(
    oe_result_t* _retval,
    oe_result_t* ocall_host_ret,
    oe_result_t* host_return_value,
    uint64_t host_session_handle,
    const void* input_data,
    size_t input_len,
    void* output_data,
    size_t output_buf_len,
    size_t* actual_output_len)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    ocall_ggml_run_inference_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.ocall_host_ret = (oe_result_t*)ocall_host_ret;
    _args.host_return_value = (oe_result_t*)host_return_value;
    _args.host_session_handle = host_session_handle;
    _args.input_data = (void*)input_data;
    _args.input_len = input_len;
    _args.output_data = (void*)output_data;
    _args.output_buf_len = output_buf_len;
    _args.actual_output_len = (size_t*)actual_output_len;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(ocall_ggml_run_inference_args_t));
    if (input_data)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.input_len);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(ocall_ggml_run_inference_args_t));
    if (ocall_host_ret)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(oe_result_t));
    if (host_return_value)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(oe_result_t));
    if (output_data)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.output_buf_len);
    if (actual_output_len)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (ocall_ggml_run_inference_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (input_data)
        OE_WRITE_IN_PARAM_WITH_BARRIER(input_data, 1, _args.input_len, void*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_ocall_ggml_run_inference,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (ocall_ggml_run_inference_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(ocall_host_ret, 1, sizeof(oe_result_t));
    OE_READ_OUT_PARAM(host_return_value, 1, sizeof(oe_result_t));
    OE_READ_OUT_PARAM(output_data, 1, _args.output_buf_len);
    OE_READ_OUT_PARAM(actual_output_len, 1, sizeof(size_t));

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t ocall_ggml_release_session(
    oe_result_t* _retval,
    oe_result_t* ocall_host_ret,
    oe_result_t* host_return_value,
    uint64_t host_session_handle)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    ocall_ggml_release_session_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.ocall_host_ret = (oe_result_t*)ocall_host_ret;
    _args.host_return_value = (oe_result_t*)host_return_value;
    _args.host_session_handle = host_session_handle;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(ocall_ggml_release_session_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(ocall_ggml_release_session_args_t));
    if (ocall_host_ret)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(oe_result_t));
    if (host_return_value)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(oe_result_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (ocall_ggml_release_session_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_ocall_ggml_release_session,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (ocall_ggml_release_session_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(ocall_host_ret, 1, sizeof(oe_result_t));
    OE_READ_OUT_PARAM(host_return_value, 1, sizeof(oe_result_t));

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_get_supported_attester_format_ids_ocall(
    oe_result_t* _retval,
    format_ids_t* format_ids)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_get_supported_attester_format_ids_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;
    uint8_t* _deepcopy_out_buffer = NULL;
    size_t _deepcopy_out_buffer_size = 0;
    size_t _deepcopy_out_buffer_offset = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.format_ids = (format_ids_t*)format_ids;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_get_supported_attester_format_ids_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_get_supported_attester_format_ids_ocall_args_t));
    if (format_ids)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(format_ids_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_get_supported_attester_format_ids_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_get_supported_attester_format_ids_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_get_supported_attester_format_ids_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    _deepcopy_out_buffer = _pargs_out->deepcopy_out_buffer;
    _deepcopy_out_buffer_size = _pargs_out->deepcopy_out_buffer_size;
    if (_deepcopy_out_buffer && _deepcopy_out_buffer_size && 
        !oe_is_within_enclave(_deepcopy_out_buffer, _deepcopy_out_buffer_size))
    {
        _result = OE_FAILURE;
        goto done;
    }

    OE_READ_OUT_PARAM(format_ids, 1, sizeof(format_ids_t));
    if (format_ids && format_ids->data)
        OE_SET_DEEPCOPY_OUT_PARAM(format_ids->data, 1, format_ids->size, void*);

    if (_deepcopy_out_buffer_offset != _deepcopy_out_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    if (_deepcopy_out_buffer)
        oe_free(_deepcopy_out_buffer);

    return _result;
}

oe_result_t oe_get_qetarget_info_ocall(
    oe_result_t* _retval,
    const oe_uuid_t* format_id,
    const void* opt_params,
    size_t opt_params_size,
    sgx_target_info_t* target_info)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_get_qetarget_info_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.format_id = (oe_uuid_t*)format_id;
    _args.opt_params = (void*)opt_params;
    _args.opt_params_size = opt_params_size;
    _args.target_info = (sgx_target_info_t*)target_info;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_get_qetarget_info_ocall_args_t));
    if (format_id)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, sizeof(oe_uuid_t));
    if (opt_params)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.opt_params_size);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_get_qetarget_info_ocall_args_t));
    if (target_info)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(sgx_target_info_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_get_qetarget_info_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (format_id)
        OE_WRITE_IN_PARAM_WITH_BARRIER(format_id, 1, sizeof(oe_uuid_t), oe_uuid_t*);
    if (opt_params)
        OE_WRITE_IN_PARAM_WITH_BARRIER(opt_params, 1, _args.opt_params_size, void*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_get_qetarget_info_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_get_qetarget_info_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(target_info, 1, sizeof(sgx_target_info_t));

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_get_quote_ocall(
    oe_result_t* _retval,
    const oe_uuid_t* format_id,
    const void* opt_params,
    size_t opt_params_size,
    const sgx_report_t* sgx_report,
    void* quote,
    size_t quote_size,
    size_t* quote_size_out)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_get_quote_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.format_id = (oe_uuid_t*)format_id;
    _args.opt_params = (void*)opt_params;
    _args.opt_params_size = opt_params_size;
    _args.sgx_report = (sgx_report_t*)sgx_report;
    _args.quote = (void*)quote;
    _args.quote_size = quote_size;
    _args.quote_size_out = (size_t*)quote_size_out;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_get_quote_ocall_args_t));
    if (format_id)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, sizeof(oe_uuid_t));
    if (opt_params)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.opt_params_size);
    if (sgx_report)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, sizeof(sgx_report_t));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_get_quote_ocall_args_t));
    if (quote)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.quote_size);
    if (quote_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_get_quote_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (format_id)
        OE_WRITE_IN_PARAM_WITH_BARRIER(format_id, 1, sizeof(oe_uuid_t), oe_uuid_t*);
    if (opt_params)
        OE_WRITE_IN_PARAM_WITH_BARRIER(opt_params, 1, _args.opt_params_size, void*);
    if (sgx_report)
        OE_WRITE_IN_PARAM_WITH_BARRIER(sgx_report, 1, sizeof(sgx_report_t), sgx_report_t*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_get_quote_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_get_quote_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(quote, 1, _args.quote_size);
    OE_READ_OUT_PARAM(quote_size_out, 1, sizeof(size_t));

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_get_quote_verification_collateral_ocall(
    oe_result_t* _retval,
    uint8_t fmspc[6],
    uint8_t collateral_provider,
    void* tcb_info,
    size_t tcb_info_size,
    size_t* tcb_info_size_out,
    void* tcb_info_issuer_chain,
    size_t tcb_info_issuer_chain_size,
    size_t* tcb_info_issuer_chain_size_out,
    void* pck_crl,
    size_t pck_crl_size,
    size_t* pck_crl_size_out,
    void* root_ca_crl,
    size_t root_ca_crl_size,
    size_t* root_ca_crl_size_out,
    void* pck_crl_issuer_chain,
    size_t pck_crl_issuer_chain_size,
    size_t* pck_crl_issuer_chain_size_out,
    void* qe_identity,
    size_t qe_identity_size,
    size_t* qe_identity_size_out,
    void* qe_identity_issuer_chain,
    size_t qe_identity_issuer_chain_size,
    size_t* qe_identity_issuer_chain_size_out)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_get_quote_verification_collateral_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.fmspc = (uint8_t*)fmspc;
    _args.collateral_provider = collateral_provider;
    _args.tcb_info = (void*)tcb_info;
    _args.tcb_info_size = tcb_info_size;
    _args.tcb_info_size_out = (size_t*)tcb_info_size_out;
    _args.tcb_info_issuer_chain = (void*)tcb_info_issuer_chain;
    _args.tcb_info_issuer_chain_size = tcb_info_issuer_chain_size;
    _args.tcb_info_issuer_chain_size_out = (size_t*)tcb_info_issuer_chain_size_out;
    _args.pck_crl = (void*)pck_crl;
    _args.pck_crl_size = pck_crl_size;
    _args.pck_crl_size_out = (size_t*)pck_crl_size_out;
    _args.root_ca_crl = (void*)root_ca_crl;
    _args.root_ca_crl_size = root_ca_crl_size;
    _args.root_ca_crl_size_out = (size_t*)root_ca_crl_size_out;
    _args.pck_crl_issuer_chain = (void*)pck_crl_issuer_chain;
    _args.pck_crl_issuer_chain_size = pck_crl_issuer_chain_size;
    _args.pck_crl_issuer_chain_size_out = (size_t*)pck_crl_issuer_chain_size_out;
    _args.qe_identity = (void*)qe_identity;
    _args.qe_identity_size = qe_identity_size;
    _args.qe_identity_size_out = (size_t*)qe_identity_size_out;
    _args.qe_identity_issuer_chain = (void*)qe_identity_issuer_chain;
    _args.qe_identity_issuer_chain_size = qe_identity_issuer_chain_size;
    _args.qe_identity_issuer_chain_size_out = (size_t*)qe_identity_issuer_chain_size_out;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_get_quote_verification_collateral_ocall_args_t));
    if (fmspc)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, sizeof(uint8_t[6]));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_get_quote_verification_collateral_ocall_args_t));
    if (tcb_info)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.tcb_info_size);
    if (tcb_info_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (tcb_info_issuer_chain)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.tcb_info_issuer_chain_size);
    if (tcb_info_issuer_chain_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (pck_crl)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.pck_crl_size);
    if (pck_crl_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (root_ca_crl)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.root_ca_crl_size);
    if (root_ca_crl_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (pck_crl_issuer_chain)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.pck_crl_issuer_chain_size);
    if (pck_crl_issuer_chain_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (qe_identity)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.qe_identity_size);
    if (qe_identity_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (qe_identity_issuer_chain)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.qe_identity_issuer_chain_size);
    if (qe_identity_issuer_chain_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_get_quote_verification_collateral_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (fmspc)
        OE_WRITE_IN_PARAM_WITH_BARRIER(fmspc, 1, sizeof(uint8_t[6]), uint8_t*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_get_quote_verification_collateral_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_get_quote_verification_collateral_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(tcb_info, 1, _args.tcb_info_size);
    OE_READ_OUT_PARAM(tcb_info_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(tcb_info_issuer_chain, 1, _args.tcb_info_issuer_chain_size);
    OE_READ_OUT_PARAM(tcb_info_issuer_chain_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(pck_crl, 1, _args.pck_crl_size);
    OE_READ_OUT_PARAM(pck_crl_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(root_ca_crl, 1, _args.root_ca_crl_size);
    OE_READ_OUT_PARAM(root_ca_crl_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(pck_crl_issuer_chain, 1, _args.pck_crl_issuer_chain_size);
    OE_READ_OUT_PARAM(pck_crl_issuer_chain_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(qe_identity, 1, _args.qe_identity_size);
    OE_READ_OUT_PARAM(qe_identity_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(qe_identity_issuer_chain, 1, _args.qe_identity_issuer_chain_size);
    OE_READ_OUT_PARAM(qe_identity_issuer_chain_size_out, 1, sizeof(size_t));

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_get_quote_verification_collateral_with_baseline_ocall(
    oe_result_t* _retval,
    uint8_t fmspc[6],
    uint8_t collateral_provider,
    void* baseline,
    size_t baseline_size,
    void* tcb_info,
    size_t tcb_info_size,
    size_t* tcb_info_size_out,
    void* tcb_info_issuer_chain,
    size_t tcb_info_issuer_chain_size,
    size_t* tcb_info_issuer_chain_size_out,
    void* pck_crl,
    size_t pck_crl_size,
    size_t* pck_crl_size_out,
    void* root_ca_crl,
    size_t root_ca_crl_size,
    size_t* root_ca_crl_size_out,
    void* pck_crl_issuer_chain,
    size_t pck_crl_issuer_chain_size,
    size_t* pck_crl_issuer_chain_size_out,
    void* qe_identity,
    size_t qe_identity_size,
    size_t* qe_identity_size_out,
    void* qe_identity_issuer_chain,
    size_t qe_identity_issuer_chain_size,
    size_t* qe_identity_issuer_chain_size_out)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_get_quote_verification_collateral_with_baseline_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.fmspc = (uint8_t*)fmspc;
    _args.collateral_provider = collateral_provider;
    _args.baseline = (void*)baseline;
    _args.baseline_size = baseline_size;
    _args.tcb_info = (void*)tcb_info;
    _args.tcb_info_size = tcb_info_size;
    _args.tcb_info_size_out = (size_t*)tcb_info_size_out;
    _args.tcb_info_issuer_chain = (void*)tcb_info_issuer_chain;
    _args.tcb_info_issuer_chain_size = tcb_info_issuer_chain_size;
    _args.tcb_info_issuer_chain_size_out = (size_t*)tcb_info_issuer_chain_size_out;
    _args.pck_crl = (void*)pck_crl;
    _args.pck_crl_size = pck_crl_size;
    _args.pck_crl_size_out = (size_t*)pck_crl_size_out;
    _args.root_ca_crl = (void*)root_ca_crl;
    _args.root_ca_crl_size = root_ca_crl_size;
    _args.root_ca_crl_size_out = (size_t*)root_ca_crl_size_out;
    _args.pck_crl_issuer_chain = (void*)pck_crl_issuer_chain;
    _args.pck_crl_issuer_chain_size = pck_crl_issuer_chain_size;
    _args.pck_crl_issuer_chain_size_out = (size_t*)pck_crl_issuer_chain_size_out;
    _args.qe_identity = (void*)qe_identity;
    _args.qe_identity_size = qe_identity_size;
    _args.qe_identity_size_out = (size_t*)qe_identity_size_out;
    _args.qe_identity_issuer_chain = (void*)qe_identity_issuer_chain;
    _args.qe_identity_issuer_chain_size = qe_identity_issuer_chain_size;
    _args.qe_identity_issuer_chain_size_out = (size_t*)qe_identity_issuer_chain_size_out;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_get_quote_verification_collateral_with_baseline_ocall_args_t));
    if (fmspc)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, sizeof(uint8_t[6]));
    if (baseline)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.baseline_size);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_get_quote_verification_collateral_with_baseline_ocall_args_t));
    if (tcb_info)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.tcb_info_size);
    if (tcb_info_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (tcb_info_issuer_chain)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.tcb_info_issuer_chain_size);
    if (tcb_info_issuer_chain_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (pck_crl)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.pck_crl_size);
    if (pck_crl_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (root_ca_crl)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.root_ca_crl_size);
    if (root_ca_crl_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (pck_crl_issuer_chain)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.pck_crl_issuer_chain_size);
    if (pck_crl_issuer_chain_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (qe_identity)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.qe_identity_size);
    if (qe_identity_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    if (qe_identity_issuer_chain)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.qe_identity_issuer_chain_size);
    if (qe_identity_issuer_chain_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_get_quote_verification_collateral_with_baseline_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (fmspc)
        OE_WRITE_IN_PARAM_WITH_BARRIER(fmspc, 1, sizeof(uint8_t[6]), uint8_t*);
    if (baseline)
        OE_WRITE_IN_PARAM_WITH_BARRIER(baseline, 1, _args.baseline_size, void*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_get_quote_verification_collateral_with_baseline_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_get_quote_verification_collateral_with_baseline_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(tcb_info, 1, _args.tcb_info_size);
    OE_READ_OUT_PARAM(tcb_info_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(tcb_info_issuer_chain, 1, _args.tcb_info_issuer_chain_size);
    OE_READ_OUT_PARAM(tcb_info_issuer_chain_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(pck_crl, 1, _args.pck_crl_size);
    OE_READ_OUT_PARAM(pck_crl_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(root_ca_crl, 1, _args.root_ca_crl_size);
    OE_READ_OUT_PARAM(root_ca_crl_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(pck_crl_issuer_chain, 1, _args.pck_crl_issuer_chain_size);
    OE_READ_OUT_PARAM(pck_crl_issuer_chain_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(qe_identity, 1, _args.qe_identity_size);
    OE_READ_OUT_PARAM(qe_identity_size_out, 1, sizeof(size_t));
    OE_READ_OUT_PARAM(qe_identity_issuer_chain, 1, _args.qe_identity_issuer_chain_size);
    OE_READ_OUT_PARAM(qe_identity_issuer_chain_size_out, 1, sizeof(size_t));

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_verify_quote_ocall(
    oe_result_t* _retval,
    const oe_uuid_t* format_id,
    const void* opt_params,
    size_t opt_params_size,
    const void* p_quote,
    uint32_t quote_size,
    const time_t expiration_check_date,
    uint32_t* p_collateral_expiration_status,
    uint32_t* p_quote_verification_result,
    void* p_qve_report_info,
    uint32_t qve_report_info_size,
    void* p_supplemental_data,
    uint32_t supplemental_data_size,
    uint32_t* p_supplemental_data_size_out,
    uint32_t collateral_version,
    const void* p_tcb_info,
    uint32_t tcb_info_size,
    const void* p_tcb_info_issuer_chain,
    uint32_t tcb_info_issuer_chain_size,
    const void* p_pck_crl,
    uint32_t pck_crl_size,
    const void* p_root_ca_crl,
    uint32_t root_ca_crl_size,
    const void* p_pck_crl_issuer_chain,
    uint32_t pck_crl_issuer_chain_size,
    const void* p_qe_identity,
    uint32_t qe_identity_size,
    const void* p_qe_identity_issuer_chain,
    uint32_t qe_identity_issuer_chain_size)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_verify_quote_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.format_id = (oe_uuid_t*)format_id;
    _args.opt_params = (void*)opt_params;
    _args.opt_params_size = opt_params_size;
    _args.p_quote = (void*)p_quote;
    _args.quote_size = quote_size;
    _args.expiration_check_date = expiration_check_date;
    _args.p_collateral_expiration_status = (uint32_t*)p_collateral_expiration_status;
    _args.p_quote_verification_result = (uint32_t*)p_quote_verification_result;
    _args.p_qve_report_info = (void*)p_qve_report_info;
    _args.qve_report_info_size = qve_report_info_size;
    _args.p_supplemental_data = (void*)p_supplemental_data;
    _args.supplemental_data_size = supplemental_data_size;
    _args.p_supplemental_data_size_out = (uint32_t*)p_supplemental_data_size_out;
    _args.collateral_version = collateral_version;
    _args.p_tcb_info = (void*)p_tcb_info;
    _args.tcb_info_size = tcb_info_size;
    _args.p_tcb_info_issuer_chain = (void*)p_tcb_info_issuer_chain;
    _args.tcb_info_issuer_chain_size = tcb_info_issuer_chain_size;
    _args.p_pck_crl = (void*)p_pck_crl;
    _args.pck_crl_size = pck_crl_size;
    _args.p_root_ca_crl = (void*)p_root_ca_crl;
    _args.root_ca_crl_size = root_ca_crl_size;
    _args.p_pck_crl_issuer_chain = (void*)p_pck_crl_issuer_chain;
    _args.pck_crl_issuer_chain_size = pck_crl_issuer_chain_size;
    _args.p_qe_identity = (void*)p_qe_identity;
    _args.qe_identity_size = qe_identity_size;
    _args.p_qe_identity_issuer_chain = (void*)p_qe_identity_issuer_chain;
    _args.qe_identity_issuer_chain_size = qe_identity_issuer_chain_size;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_verify_quote_ocall_args_t));
    if (format_id)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, sizeof(oe_uuid_t));
    if (opt_params)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.opt_params_size);
    if (p_quote)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.quote_size);
    if (p_qve_report_info)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.qve_report_info_size);
    if (p_tcb_info)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.tcb_info_size);
    if (p_tcb_info_issuer_chain)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.tcb_info_issuer_chain_size);
    if (p_pck_crl)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.pck_crl_size);
    if (p_root_ca_crl)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.root_ca_crl_size);
    if (p_pck_crl_issuer_chain)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.pck_crl_issuer_chain_size);
    if (p_qe_identity)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.qe_identity_size);
    if (p_qe_identity_issuer_chain)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.qe_identity_issuer_chain_size);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_verify_quote_ocall_args_t));
    if (p_collateral_expiration_status)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(uint32_t));
    if (p_quote_verification_result)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(uint32_t));
    if (p_qve_report_info)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.qve_report_info_size);
    if (p_supplemental_data)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.supplemental_data_size);
    if (p_supplemental_data_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(uint32_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_verify_quote_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (format_id)
        OE_WRITE_IN_PARAM_WITH_BARRIER(format_id, 1, sizeof(oe_uuid_t), oe_uuid_t*);
    if (opt_params)
        OE_WRITE_IN_PARAM_WITH_BARRIER(opt_params, 1, _args.opt_params_size, void*);
    if (p_quote)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_quote, 1, _args.quote_size, void*);
    if (p_qve_report_info)
        OE_WRITE_IN_OUT_PARAM_WITH_BARRIER(p_qve_report_info, 1, _args.qve_report_info_size, void*);
    if (p_tcb_info)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_tcb_info, 1, _args.tcb_info_size, void*);
    if (p_tcb_info_issuer_chain)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_tcb_info_issuer_chain, 1, _args.tcb_info_issuer_chain_size, void*);
    if (p_pck_crl)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_pck_crl, 1, _args.pck_crl_size, void*);
    if (p_root_ca_crl)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_root_ca_crl, 1, _args.root_ca_crl_size, void*);
    if (p_pck_crl_issuer_chain)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_pck_crl_issuer_chain, 1, _args.pck_crl_issuer_chain_size, void*);
    if (p_qe_identity)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_qe_identity, 1, _args.qe_identity_size, void*);
    if (p_qe_identity_issuer_chain)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_qe_identity_issuer_chain, 1, _args.qe_identity_issuer_chain_size, void*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_verify_quote_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_verify_quote_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(p_collateral_expiration_status, 1, sizeof(uint32_t));
    OE_READ_OUT_PARAM(p_quote_verification_result, 1, sizeof(uint32_t));
    OE_READ_IN_OUT_PARAM(p_qve_report_info, 1, _args.qve_report_info_size);
    OE_READ_OUT_PARAM(p_supplemental_data, 1, _args.supplemental_data_size);
    OE_READ_OUT_PARAM(p_supplemental_data_size_out, 1, sizeof(uint32_t));

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_sgx_get_cpuid_table_ocall(
    oe_result_t* _retval,
    void* cpuid_table_buffer,
    size_t cpuid_table_buffer_size)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_sgx_get_cpuid_table_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.cpuid_table_buffer = (void*)cpuid_table_buffer;
    _args.cpuid_table_buffer_size = cpuid_table_buffer_size;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_sgx_get_cpuid_table_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_sgx_get_cpuid_table_ocall_args_t));
    if (cpuid_table_buffer)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.cpuid_table_buffer_size);
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_sgx_get_cpuid_table_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_sgx_get_cpuid_table_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_sgx_get_cpuid_table_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(cpuid_table_buffer, 1, _args.cpuid_table_buffer_size);

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_sgx_backtrace_symbols_ocall(
    oe_result_t* _retval,
    oe_enclave_t* oe_enclave,
    const uint64_t* buffer,
    size_t size,
    void* symbols_buffer,
    size_t symbols_buffer_size,
    size_t* symbols_buffer_size_out)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_sgx_backtrace_symbols_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.oe_enclave = (oe_enclave_t*)oe_enclave;
    _args.buffer = (uint64_t*)buffer;
    _args.size = size;
    _args.symbols_buffer = (void*)symbols_buffer;
    _args.symbols_buffer_size = symbols_buffer_size;
    _args.symbols_buffer_size_out = (size_t*)symbols_buffer_size_out;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_sgx_backtrace_symbols_ocall_args_t));
    if (buffer)
        OE_ADD_ARG_SIZE(_input_buffer_size, _args.size, sizeof(uint64_t));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_sgx_backtrace_symbols_ocall_args_t));
    if (symbols_buffer)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.symbols_buffer_size);
    if (symbols_buffer_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_sgx_backtrace_symbols_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (buffer)
        OE_WRITE_IN_PARAM_WITH_BARRIER(buffer, _args.size, sizeof(uint64_t), uint64_t*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_sgx_backtrace_symbols_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_sgx_backtrace_symbols_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(symbols_buffer, 1, _args.symbols_buffer_size);
    OE_READ_OUT_PARAM(symbols_buffer_size_out, 1, sizeof(size_t));

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_sgx_log_backtrace_ocall(
    oe_result_t* _retval,
    oe_enclave_t* oe_enclave,
    oe_log_level_t level,
    const uint64_t* buffer,
    size_t size)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_sgx_log_backtrace_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.oe_enclave = (oe_enclave_t*)oe_enclave;
    _args.level = level;
    _args.buffer = (uint64_t*)buffer;
    _args.size = size;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_sgx_log_backtrace_ocall_args_t));
    if (buffer)
        OE_ADD_ARG_SIZE(_input_buffer_size, _args.size, sizeof(uint64_t));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_sgx_log_backtrace_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_sgx_log_backtrace_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (buffer)
        OE_WRITE_IN_PARAM_WITH_BARRIER(buffer, _args.size, sizeof(uint64_t), uint64_t*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_sgx_log_backtrace_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_sgx_log_backtrace_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_sgx_thread_wake_wait_ocall(
    oe_enclave_t* oe_enclave,
    uint64_t waiter_tcs,
    uint64_t self_tcs)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_sgx_thread_wake_wait_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.oe_enclave = (oe_enclave_t*)oe_enclave;
    _args.waiter_tcs = waiter_tcs;
    _args.self_tcs = self_tcs;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_sgx_thread_wake_wait_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_sgx_thread_wake_wait_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_sgx_thread_wake_wait_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_sgx_thread_wake_wait_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_sgx_thread_wake_wait_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */

    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_sgx_wake_switchless_worker_ocall(oe_host_worker_context_t* context)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_sgx_wake_switchless_worker_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.context = (oe_host_worker_context_t*)context;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_sgx_wake_switchless_worker_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_sgx_wake_switchless_worker_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_sgx_wake_switchless_worker_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_sgx_wake_switchless_worker_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_sgx_wake_switchless_worker_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */

    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_sgx_sleep_switchless_worker_ocall(oe_enclave_worker_context_t* context)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_sgx_sleep_switchless_worker_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.context = (oe_enclave_worker_context_t*)context;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_sgx_sleep_switchless_worker_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_sgx_sleep_switchless_worker_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_sgx_sleep_switchless_worker_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_sgx_sleep_switchless_worker_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_sgx_sleep_switchless_worker_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */

    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_get_tdx_quote_verification_collateral_ocall(
    oe_result_t* _retval,
    const void* p_quote,
    uint32_t quote_size,
    tdx_quote_collateral_t* collateral)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_get_tdx_quote_verification_collateral_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;
    uint8_t* _deepcopy_out_buffer = NULL;
    size_t _deepcopy_out_buffer_size = 0;
    size_t _deepcopy_out_buffer_offset = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.p_quote = (void*)p_quote;
    _args.quote_size = quote_size;
    _args.collateral = (tdx_quote_collateral_t*)collateral;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_get_tdx_quote_verification_collateral_ocall_args_t));
    if (p_quote)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.quote_size);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_get_tdx_quote_verification_collateral_ocall_args_t));
    if (collateral)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(tdx_quote_collateral_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_get_tdx_quote_verification_collateral_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (p_quote)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_quote, 1, _args.quote_size, void*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_get_tdx_quote_verification_collateral_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_get_tdx_quote_verification_collateral_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    _deepcopy_out_buffer = _pargs_out->deepcopy_out_buffer;
    _deepcopy_out_buffer_size = _pargs_out->deepcopy_out_buffer_size;
    if (_deepcopy_out_buffer && _deepcopy_out_buffer_size && 
        !oe_is_within_enclave(_deepcopy_out_buffer, _deepcopy_out_buffer_size))
    {
        _result = OE_FAILURE;
        goto done;
    }

    OE_READ_OUT_PARAM(collateral, 1, sizeof(tdx_quote_collateral_t));
    if (collateral && collateral->data)
        OE_SET_DEEPCOPY_OUT_PARAM(collateral->data, 1, collateral->size, uint8_t*);

    if (_deepcopy_out_buffer_offset != _deepcopy_out_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    if (_deepcopy_out_buffer)
        oe_free(_deepcopy_out_buffer);

    return _result;
}

oe_result_t oe_verify_tdx_quote_ocall(
    oe_result_t* _retval,
    const oe_uuid_t* format_id,
    const void* opt_params,
    size_t opt_params_size,
    const void* p_quote,
    uint32_t quote_size,
    const void* p_endorsements,
    uint32_t endorsements_size,
    const time_t expiration_check_date,
    uint32_t* p_collateral_expiration_status,
    uint32_t* p_quote_verification_result,
    void* p_qve_report_info,
    uint32_t qve_report_info_size,
    void* p_supplemental_data,
    uint32_t supplemental_data_size,
    uint32_t* p_supplemental_data_size_out)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_verify_tdx_quote_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.format_id = (oe_uuid_t*)format_id;
    _args.opt_params = (void*)opt_params;
    _args.opt_params_size = opt_params_size;
    _args.p_quote = (void*)p_quote;
    _args.quote_size = quote_size;
    _args.p_endorsements = (void*)p_endorsements;
    _args.endorsements_size = endorsements_size;
    _args.expiration_check_date = expiration_check_date;
    _args.p_collateral_expiration_status = (uint32_t*)p_collateral_expiration_status;
    _args.p_quote_verification_result = (uint32_t*)p_quote_verification_result;
    _args.p_qve_report_info = (void*)p_qve_report_info;
    _args.qve_report_info_size = qve_report_info_size;
    _args.p_supplemental_data = (void*)p_supplemental_data;
    _args.supplemental_data_size = supplemental_data_size;
    _args.p_supplemental_data_size_out = (uint32_t*)p_supplemental_data_size_out;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_verify_tdx_quote_ocall_args_t));
    if (format_id)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, sizeof(oe_uuid_t));
    if (opt_params)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.opt_params_size);
    if (p_quote)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.quote_size);
    if (p_endorsements)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.endorsements_size);
    if (p_qve_report_info)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.qve_report_info_size);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_verify_tdx_quote_ocall_args_t));
    if (p_collateral_expiration_status)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(uint32_t));
    if (p_quote_verification_result)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(uint32_t));
    if (p_qve_report_info)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.qve_report_info_size);
    if (p_supplemental_data)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, _args.supplemental_data_size);
    if (p_supplemental_data_size_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(uint32_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_verify_tdx_quote_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (format_id)
        OE_WRITE_IN_PARAM_WITH_BARRIER(format_id, 1, sizeof(oe_uuid_t), oe_uuid_t*);
    if (opt_params)
        OE_WRITE_IN_PARAM_WITH_BARRIER(opt_params, 1, _args.opt_params_size, void*);
    if (p_quote)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_quote, 1, _args.quote_size, void*);
    if (p_endorsements)
        OE_WRITE_IN_PARAM_WITH_BARRIER(p_endorsements, 1, _args.endorsements_size, void*);
    if (p_qve_report_info)
        OE_WRITE_IN_OUT_PARAM_WITH_BARRIER(p_qve_report_info, 1, _args.qve_report_info_size, void*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_verify_tdx_quote_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_verify_tdx_quote_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(p_collateral_expiration_status, 1, sizeof(uint32_t));
    OE_READ_OUT_PARAM(p_quote_verification_result, 1, sizeof(uint32_t));
    OE_READ_IN_OUT_PARAM(p_qve_report_info, 1, _args.qve_report_info_size);
    OE_READ_OUT_PARAM(p_supplemental_data, 1, _args.supplemental_data_size);
    OE_READ_OUT_PARAM(p_supplemental_data_size_out, 1, sizeof(uint32_t));

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_log_is_supported_ocall(
    )
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_log_is_supported_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_log_is_supported_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_log_is_supported_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_log_is_supported_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_log_is_supported_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_log_is_supported_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */

    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_log_ocall(
    uint32_t log_level,
    const char* message)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_log_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.log_level = log_level;
    _args.message = (char*)message;
    _args.message_len = (message) ? (oe_strlen(message) + 1) : 0;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_log_ocall_args_t));
    if (message)
        OE_ADD_ARG_SIZE(_input_buffer_size, _args.message_len, sizeof(char));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_log_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_log_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (message)
        OE_WRITE_IN_PARAM_WITH_BARRIER(message, _args.message_len, sizeof(char), char*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_log_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_log_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */

    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

oe_result_t oe_write_ocall(
    int device,
    const char* str,
    size_t maxlen)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    oe_write_ocall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    uint8_t* _output_buffer_trusted = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.device = device;
    _args.str = (char*)str;
    _args.str_len = (str) ? (oe_strlen(str) + 1) : 0;
    _args.maxlen = maxlen;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_write_ocall_args_t));
    if (str)
        OE_ADD_ARG_SIZE(_input_buffer_size, _args.str_len, sizeof(char));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_write_ocall_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_write_ocall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (str)
        OE_WRITE_IN_PARAM_WITH_BARRIER(str, _args.str_len, sizeof(char), char*);
    
    /* Copy args structure (now filled) to input buffer. */
    oe_memcpy_with_barrier(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             enclave_fcn_id_oe_write_ocall,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Allocate an enclave buffer for reading the host buffer */
    if (oe_edger8r_secure_unserialize)
    {
        _output_buffer_trusted = (uint8_t*)oe_malloc(_output_buffer_size);
        if (!_output_buffer_trusted)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }

        /* _output_buffer and _output_buffer_size should be always 8-byte aligned */
        if (((uint64_t)_output_buffer % 8) != 0 || (_output_buffer_size % 8) != 0)
        {
            _result = OE_FAILURE;
            goto done;
        }
        oe_memcpy_aligned(_output_buffer_trusted, _output_buffer, _output_buffer_size);

        /* Now _output_buffer points to the enclave memory */
        _output_buffer = _output_buffer_trusted;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_write_ocall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */

    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);

    if (_output_buffer_trusted)
        oe_free(_output_buffer_trusted);

    return _result;
}

OE_EXTERNC_END
