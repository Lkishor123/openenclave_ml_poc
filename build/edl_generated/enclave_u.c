/*
 *  This file is auto generated by oeedger8r. DO NOT EDIT.
 */
#include "enclave_u.h"

#include <openenclave/edger8r/host.h>

OE_EXTERNC_BEGIN

/**** Trusted function IDs. ****/
enum
{
    enclave_fcn_id_initialize_enclave_ml_context = 0,
    enclave_fcn_id_enclave_infer = 1,
    enclave_fcn_id_terminate_enclave_ml_context = 2,
    enclave_fcn_id_oe_get_sgx_report_ecall = 3,
    enclave_fcn_id_oe_get_report_v2_ecall = 4,
    enclave_fcn_id_oe_verify_local_report_ecall = 5,
    enclave_fcn_id_oe_sgx_init_context_switchless_ecall = 6,
    enclave_fcn_id_oe_sgx_switchless_enclave_worker_thread_ecall = 7,
    enclave_fcn_id_oe_log_init_ecall = 8,
    enclave_fcn_id_trusted_call_id_max = OE_ENUM_MAX
};

/**** Trusted function names. ****/
static const oe_ecall_info_t _enclave_ecall_info_table[] = 
{
    { "initialize_enclave_ml_context" },
    { "enclave_infer" },
    { "terminate_enclave_ml_context" },
    { "oe_get_sgx_report_ecall" },
    { "oe_get_report_v2_ecall" },
    { "oe_verify_local_report_ecall" },
    { "oe_sgx_init_context_switchless_ecall" },
    { "oe_sgx_switchless_enclave_worker_thread_ecall" },
    { "oe_log_init_ecall" },
};

/**** ECALL marshalling structs. ****/
typedef struct _initialize_enclave_ml_context_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    unsigned char* model_data;
    size_t model_size;
    uint64_t* enclave_session_handle;
} initialize_enclave_ml_context_args_t;

typedef struct _enclave_infer_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint64_t enclave_session_handle;
    int64_t* input_data;
    size_t input_data_byte_size;
    float* output_buffer;
    size_t output_buffer_byte_size;
    size_t* actual_output_size_bytes_out;
} enclave_infer_args_t;

typedef struct _terminate_enclave_ml_context_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint64_t enclave_session_handle;
} terminate_enclave_ml_context_args_t;

typedef struct _oe_get_sgx_report_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    void* opt_params;
    size_t opt_params_size;
    sgx_report_t* report;
} oe_get_sgx_report_ecall_args_t;

typedef struct _oe_get_report_v2_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint32_t flags;
    void* opt_params;
    size_t opt_params_size;
    oe_report_buffer_t* report;
} oe_get_report_v2_ecall_args_t;

typedef struct _oe_verify_local_report_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint8_t* report;
    size_t report_size;
    oe_report_t* parsed_report;
} oe_verify_local_report_ecall_args_t;

typedef struct _oe_sgx_init_context_switchless_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_host_worker_context_t* host_worker_contexts;
    uint64_t num_host_workers;
} oe_sgx_init_context_switchless_ecall_args_t;

typedef struct _oe_sgx_switchless_enclave_worker_thread_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_enclave_worker_context_t* context;
} oe_sgx_switchless_enclave_worker_thread_ecall_args_t;

typedef struct _oe_log_init_ecall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    char* enclave_path;
    size_t enclave_path_len;
    uint32_t log_level;
} oe_log_init_ecall_args_t;

/**** ECALL function wrappers. ****/

oe_result_t enclave_initialize_enclave_ml_context(
    oe_enclave_t* enclave,
    oe_result_t* _retval,
    const unsigned char* model_data,
    size_t model_size,
    uint64_t* enclave_session_handle)
{
    oe_result_t _result = OE_FAILURE;

    static uint64_t global_id = OE_GLOBAL_ECALL_ID_NULL;

    /* Marshalling struct. */
    initialize_enclave_ml_context_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.model_data = (unsigned char*)model_data;
    _args.model_size = model_size;
    _args.enclave_session_handle = (uint64_t*)enclave_session_handle;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(initialize_enclave_ml_context_args_t));
    if (model_data)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.model_size);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(initialize_enclave_ml_context_args_t));
    if (enclave_session_handle)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(uint64_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_malloc(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (initialize_enclave_ml_context_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (model_data)
        OE_WRITE_IN_PARAM(model_data, 1, _args.model_size, unsigned char*);
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call enclave function. */
    if ((_result = oe_call_enclave_function(
             enclave,
             &global_id,
             _enclave_ecall_info_table[enclave_fcn_id_initialize_enclave_ml_context].name,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (initialize_enclave_ml_context_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(enclave_session_handle, 1, sizeof(uint64_t));

    _result = OE_OK;

done:
    if (_buffer)
        oe_free(_buffer);

    return _result;
}

OE_WEAK_ALIAS(enclave_initialize_enclave_ml_context, initialize_enclave_ml_context);

oe_result_t enclave_enclave_infer(
    oe_enclave_t* enclave,
    oe_result_t* _retval,
    uint64_t enclave_session_handle,
    const int64_t* input_data,
    size_t input_data_byte_size,
    float* output_buffer,
    size_t output_buffer_byte_size,
    size_t* actual_output_size_bytes_out)
{
    oe_result_t _result = OE_FAILURE;

    static uint64_t global_id = OE_GLOBAL_ECALL_ID_NULL;

    /* Marshalling struct. */
    enclave_infer_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.enclave_session_handle = enclave_session_handle;
    _args.input_data = (int64_t*)input_data;
    _args.input_data_byte_size = input_data_byte_size;
    _args.output_buffer = (float*)output_buffer;
    _args.output_buffer_byte_size = output_buffer_byte_size;
    _args.actual_output_size_bytes_out = (size_t*)actual_output_size_bytes_out;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(enclave_infer_args_t));
    if (input_data)
        OE_ADD_ARG_SIZE(_input_buffer_size, _args.input_data_byte_size, sizeof(int64_t));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(enclave_infer_args_t));
    if (output_buffer)
        OE_ADD_ARG_SIZE(_output_buffer_size, _args.output_buffer_byte_size, sizeof(float));
    if (actual_output_size_bytes_out)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(size_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_malloc(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (enclave_infer_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (input_data)
        OE_WRITE_IN_PARAM(input_data, _args.input_data_byte_size, sizeof(int64_t), int64_t*);
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call enclave function. */
    if ((_result = oe_call_enclave_function(
             enclave,
             &global_id,
             _enclave_ecall_info_table[enclave_fcn_id_enclave_infer].name,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (enclave_infer_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(output_buffer, _args.output_buffer_byte_size, sizeof(float));
    OE_READ_OUT_PARAM(actual_output_size_bytes_out, 1, sizeof(size_t));

    _result = OE_OK;

done:
    if (_buffer)
        oe_free(_buffer);

    return _result;
}

OE_WEAK_ALIAS(enclave_enclave_infer, enclave_infer);

oe_result_t enclave_terminate_enclave_ml_context(
    oe_enclave_t* enclave,
    oe_result_t* _retval,
    uint64_t enclave_session_handle)
{
    oe_result_t _result = OE_FAILURE;

    static uint64_t global_id = OE_GLOBAL_ECALL_ID_NULL;

    /* Marshalling struct. */
    terminate_enclave_ml_context_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.enclave_session_handle = enclave_session_handle;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(terminate_enclave_ml_context_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(terminate_enclave_ml_context_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_malloc(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (terminate_enclave_ml_context_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call enclave function. */
    if ((_result = oe_call_enclave_function(
             enclave,
             &global_id,
             _enclave_ecall_info_table[enclave_fcn_id_terminate_enclave_ml_context].name,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (terminate_enclave_ml_context_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    /* There were no out nor in-out parameters. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free(_buffer);

    return _result;
}

OE_WEAK_ALIAS(enclave_terminate_enclave_ml_context, terminate_enclave_ml_context);

oe_result_t enclave_oe_get_sgx_report_ecall(
    oe_enclave_t* enclave,
    oe_result_t* _retval,
    const void* opt_params,
    size_t opt_params_size,
    sgx_report_t* report)
{
    oe_result_t _result = OE_FAILURE;

    static uint64_t global_id = OE_GLOBAL_ECALL_ID_NULL;

    /* Marshalling struct. */
    oe_get_sgx_report_ecall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.opt_params = (void*)opt_params;
    _args.opt_params_size = opt_params_size;
    _args.report = (sgx_report_t*)report;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_get_sgx_report_ecall_args_t));
    if (opt_params)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.opt_params_size);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_get_sgx_report_ecall_args_t));
    if (report)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(sgx_report_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_malloc(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_get_sgx_report_ecall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (opt_params)
        OE_WRITE_IN_PARAM(opt_params, 1, _args.opt_params_size, void*);
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call enclave function. */
    if ((_result = oe_call_enclave_function(
             enclave,
             &global_id,
             _enclave_ecall_info_table[enclave_fcn_id_oe_get_sgx_report_ecall].name,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_get_sgx_report_ecall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(report, 1, sizeof(sgx_report_t));

    _result = OE_OK;

done:
    if (_buffer)
        oe_free(_buffer);

    return _result;
}

OE_WEAK_ALIAS(enclave_oe_get_sgx_report_ecall, oe_get_sgx_report_ecall);

oe_result_t enclave_oe_get_report_v2_ecall(
    oe_enclave_t* enclave,
    oe_result_t* _retval,
    uint32_t flags,
    const void* opt_params,
    size_t opt_params_size,
    oe_report_buffer_t* report)
{
    oe_result_t _result = OE_FAILURE;

    static uint64_t global_id = OE_GLOBAL_ECALL_ID_NULL;

    /* Marshalling struct. */
    oe_get_report_v2_ecall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;
    uint8_t* _deepcopy_out_buffer = NULL;
    size_t _deepcopy_out_buffer_size = 0;
    size_t _deepcopy_out_buffer_offset = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.flags = flags;
    _args.opt_params = (void*)opt_params;
    _args.opt_params_size = opt_params_size;
    _args.report = (oe_report_buffer_t*)report;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_get_report_v2_ecall_args_t));
    if (opt_params)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.opt_params_size);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_get_report_v2_ecall_args_t));
    if (report)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(oe_report_buffer_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_malloc(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_get_report_v2_ecall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (opt_params)
        OE_WRITE_IN_PARAM(opt_params, 1, _args.opt_params_size, void*);
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call enclave function. */
    if ((_result = oe_call_enclave_function(
             enclave,
             &global_id,
             _enclave_ecall_info_table[enclave_fcn_id_oe_get_report_v2_ecall].name,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_get_report_v2_ecall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    _deepcopy_out_buffer = _pargs_out->deepcopy_out_buffer;
    _deepcopy_out_buffer_size = _pargs_out->deepcopy_out_buffer_size;

    OE_READ_OUT_PARAM(report, 1, sizeof(oe_report_buffer_t));
    if (report && report->buffer)
        OE_SET_DEEPCOPY_OUT_PARAM(report->buffer, 1, report->size, uint8_t*);

    if (_deepcopy_out_buffer_offset != _deepcopy_out_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    _result = OE_OK;

done:
    if (_buffer)
        oe_free(_buffer);

    if (_deepcopy_out_buffer)
        oe_free(_deepcopy_out_buffer);

    return _result;
}

OE_WEAK_ALIAS(enclave_oe_get_report_v2_ecall, oe_get_report_v2_ecall);

oe_result_t enclave_oe_verify_local_report_ecall(
    oe_enclave_t* enclave,
    oe_result_t* _retval,
    const uint8_t* report,
    size_t report_size,
    oe_report_t* parsed_report)
{
    oe_result_t _result = OE_FAILURE;

    static uint64_t global_id = OE_GLOBAL_ECALL_ID_NULL;

    /* Marshalling struct. */
    oe_verify_local_report_ecall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.report = (uint8_t*)report;
    _args.report_size = report_size;
    _args.parsed_report = (oe_report_t*)parsed_report;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_verify_local_report_ecall_args_t));
    if (report)
        OE_ADD_ARG_SIZE(_input_buffer_size, 1, _args.report_size);
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_verify_local_report_ecall_args_t));
    if (parsed_report)
        OE_ADD_ARG_SIZE(_output_buffer_size, 1, sizeof(oe_report_t));
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_malloc(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_verify_local_report_ecall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (report)
        OE_WRITE_IN_PARAM(report, 1, _args.report_size, uint8_t*);
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call enclave function. */
    if ((_result = oe_call_enclave_function(
             enclave,
             &global_id,
             _enclave_ecall_info_table[enclave_fcn_id_oe_verify_local_report_ecall].name,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_verify_local_report_ecall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    OE_READ_OUT_PARAM(parsed_report, 1, sizeof(oe_report_t));

    _result = OE_OK;

done:
    if (_buffer)
        oe_free(_buffer);

    return _result;
}

OE_WEAK_ALIAS(enclave_oe_verify_local_report_ecall, oe_verify_local_report_ecall);

oe_result_t enclave_oe_sgx_init_context_switchless_ecall(
    oe_enclave_t* enclave,
    oe_result_t* _retval,
    oe_host_worker_context_t* host_worker_contexts,
    uint64_t num_host_workers)
{
    oe_result_t _result = OE_FAILURE;

    static uint64_t global_id = OE_GLOBAL_ECALL_ID_NULL;

    /* Marshalling struct. */
    oe_sgx_init_context_switchless_ecall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.host_worker_contexts = (oe_host_worker_context_t*)host_worker_contexts;
    _args.num_host_workers = num_host_workers;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_sgx_init_context_switchless_ecall_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_sgx_init_context_switchless_ecall_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_malloc(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_sgx_init_context_switchless_ecall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call enclave function. */
    if ((_result = oe_call_enclave_function(
             enclave,
             &global_id,
             _enclave_ecall_info_table[enclave_fcn_id_oe_sgx_init_context_switchless_ecall].name,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_sgx_init_context_switchless_ecall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    *_retval = _pargs_out->oe_retval;

    /* There were no out nor in-out parameters. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free(_buffer);

    return _result;
}

OE_WEAK_ALIAS(enclave_oe_sgx_init_context_switchless_ecall, oe_sgx_init_context_switchless_ecall);

oe_result_t enclave_oe_sgx_switchless_enclave_worker_thread_ecall(
    oe_enclave_t* enclave,
    oe_enclave_worker_context_t* context)
{
    oe_result_t _result = OE_FAILURE;

    static uint64_t global_id = OE_GLOBAL_ECALL_ID_NULL;

    /* Marshalling struct. */
    oe_sgx_switchless_enclave_worker_thread_ecall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.context = (oe_enclave_worker_context_t*)context;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_sgx_switchless_enclave_worker_thread_ecall_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_sgx_switchless_enclave_worker_thread_ecall_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_malloc(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_sgx_switchless_enclave_worker_thread_ecall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call enclave function. */
    if ((_result = oe_call_enclave_function(
             enclave,
             &global_id,
             _enclave_ecall_info_table[enclave_fcn_id_oe_sgx_switchless_enclave_worker_thread_ecall].name,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_sgx_switchless_enclave_worker_thread_ecall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */

    /* There were no out nor in-out parameters. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free(_buffer);

    return _result;
}

OE_WEAK_ALIAS(enclave_oe_sgx_switchless_enclave_worker_thread_ecall, oe_sgx_switchless_enclave_worker_thread_ecall);

oe_result_t enclave_oe_log_init_ecall(
    oe_enclave_t* enclave,
    const char* enclave_path,
    uint32_t log_level)
{
    oe_result_t _result = OE_FAILURE;

    static uint64_t global_id = OE_GLOBAL_ECALL_ID_NULL;

    /* Marshalling struct. */
    oe_log_init_ecall_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.enclave_path = (char*)enclave_path;
    _args.enclave_path_len = (enclave_path) ? (oe_strlen(enclave_path) + 1) : 0;
    _args.log_level = log_level;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(oe_log_init_ecall_args_t));
    if (enclave_path)
        OE_ADD_ARG_SIZE(_input_buffer_size, _args.enclave_path_len, sizeof(char));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(oe_log_init_ecall_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_malloc(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (oe_log_init_ecall_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (enclave_path)
        OE_WRITE_IN_PARAM(enclave_path, _args.enclave_path_len, sizeof(char), char*);
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call enclave function. */
    if ((_result = oe_call_enclave_function(
             enclave,
             &global_id,
             _enclave_ecall_info_table[enclave_fcn_id_oe_log_init_ecall].name,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Setup output arg struct pointer. */
    _pargs_out = (oe_log_init_ecall_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    /* Check if the call succeeded. */
    if ((_result = _pargs_out->oe_result) != OE_OK)
        goto done;

    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */

    /* There were no out nor in-out parameters. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free(_buffer);

    return _result;
}

OE_WEAK_ALIAS(enclave_oe_log_init_ecall, oe_log_init_ecall);

/**** Untrusted function IDs. ****/
enum
{
    enclave_fcn_id_ocall_onnx_load_model = 0,
    enclave_fcn_id_ocall_onnx_run_inference = 1,
    enclave_fcn_id_ocall_onnx_release_session = 2,
    enclave_fcn_id_oe_get_supported_attester_format_ids_ocall = 3,
    enclave_fcn_id_oe_get_qetarget_info_ocall = 4,
    enclave_fcn_id_oe_get_quote_ocall = 5,
    enclave_fcn_id_oe_get_quote_verification_collateral_ocall = 6,
    enclave_fcn_id_oe_get_quote_verification_collateral_with_baseline_ocall = 7,
    enclave_fcn_id_oe_verify_quote_ocall = 8,
    enclave_fcn_id_oe_sgx_get_cpuid_table_ocall = 9,
    enclave_fcn_id_oe_sgx_backtrace_symbols_ocall = 10,
    enclave_fcn_id_oe_sgx_log_backtrace_ocall = 11,
    enclave_fcn_id_oe_sgx_thread_wake_wait_ocall = 12,
    enclave_fcn_id_oe_sgx_wake_switchless_worker_ocall = 13,
    enclave_fcn_id_oe_sgx_sleep_switchless_worker_ocall = 14,
    enclave_fcn_id_oe_get_tdx_quote_verification_collateral_ocall = 15,
    enclave_fcn_id_oe_verify_tdx_quote_ocall = 16,
    enclave_fcn_id_oe_log_is_supported_ocall = 17,
    enclave_fcn_id_oe_log_ocall = 18,
    enclave_fcn_id_oe_write_ocall = 19,
    enclave_fcn_id_untrusted_call_max = OE_ENUM_MAX
};

/**** OCALL marshalling structs. ****/
typedef struct _ocall_onnx_load_model_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_result_t* ocall_host_ret;
    oe_result_t* host_return_value;
    uint64_t* host_session_handle;
    unsigned char* model_data;
    size_t model_data_len;
} ocall_onnx_load_model_args_t;

typedef struct _ocall_onnx_run_inference_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_result_t* ocall_host_ret;
    oe_result_t* host_return_value;
    uint64_t host_session_handle;
    void* input_data;
    size_t input_len;
    void* output_data;
    size_t output_buf_len;
    size_t* actual_output_len;
} ocall_onnx_run_inference_args_t;

typedef struct _ocall_onnx_release_session_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_result_t* ocall_host_ret;
    oe_result_t* host_return_value;
    uint64_t host_session_handle;
} ocall_onnx_release_session_args_t;

typedef struct _oe_get_supported_attester_format_ids_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    format_ids_t* format_ids;
} oe_get_supported_attester_format_ids_ocall_args_t;

typedef struct _oe_get_qetarget_info_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_uuid_t* format_id;
    void* opt_params;
    size_t opt_params_size;
    sgx_target_info_t* target_info;
} oe_get_qetarget_info_ocall_args_t;

typedef struct _oe_get_quote_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_uuid_t* format_id;
    void* opt_params;
    size_t opt_params_size;
    sgx_report_t* sgx_report;
    void* quote;
    size_t quote_size;
    size_t* quote_size_out;
} oe_get_quote_ocall_args_t;

typedef struct _oe_get_quote_verification_collateral_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint8_t* fmspc;
    uint8_t collateral_provider;
    void* tcb_info;
    size_t tcb_info_size;
    size_t* tcb_info_size_out;
    void* tcb_info_issuer_chain;
    size_t tcb_info_issuer_chain_size;
    size_t* tcb_info_issuer_chain_size_out;
    void* pck_crl;
    size_t pck_crl_size;
    size_t* pck_crl_size_out;
    void* root_ca_crl;
    size_t root_ca_crl_size;
    size_t* root_ca_crl_size_out;
    void* pck_crl_issuer_chain;
    size_t pck_crl_issuer_chain_size;
    size_t* pck_crl_issuer_chain_size_out;
    void* qe_identity;
    size_t qe_identity_size;
    size_t* qe_identity_size_out;
    void* qe_identity_issuer_chain;
    size_t qe_identity_issuer_chain_size;
    size_t* qe_identity_issuer_chain_size_out;
} oe_get_quote_verification_collateral_ocall_args_t;

typedef struct _oe_get_quote_verification_collateral_with_baseline_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    uint8_t* fmspc;
    uint8_t collateral_provider;
    void* baseline;
    size_t baseline_size;
    void* tcb_info;
    size_t tcb_info_size;
    size_t* tcb_info_size_out;
    void* tcb_info_issuer_chain;
    size_t tcb_info_issuer_chain_size;
    size_t* tcb_info_issuer_chain_size_out;
    void* pck_crl;
    size_t pck_crl_size;
    size_t* pck_crl_size_out;
    void* root_ca_crl;
    size_t root_ca_crl_size;
    size_t* root_ca_crl_size_out;
    void* pck_crl_issuer_chain;
    size_t pck_crl_issuer_chain_size;
    size_t* pck_crl_issuer_chain_size_out;
    void* qe_identity;
    size_t qe_identity_size;
    size_t* qe_identity_size_out;
    void* qe_identity_issuer_chain;
    size_t qe_identity_issuer_chain_size;
    size_t* qe_identity_issuer_chain_size_out;
} oe_get_quote_verification_collateral_with_baseline_ocall_args_t;

typedef struct _oe_verify_quote_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_uuid_t* format_id;
    void* opt_params;
    size_t opt_params_size;
    void* p_quote;
    uint32_t quote_size;
    time_t expiration_check_date;
    uint32_t* p_collateral_expiration_status;
    uint32_t* p_quote_verification_result;
    void* p_qve_report_info;
    uint32_t qve_report_info_size;
    void* p_supplemental_data;
    uint32_t supplemental_data_size;
    uint32_t* p_supplemental_data_size_out;
    uint32_t collateral_version;
    void* p_tcb_info;
    uint32_t tcb_info_size;
    void* p_tcb_info_issuer_chain;
    uint32_t tcb_info_issuer_chain_size;
    void* p_pck_crl;
    uint32_t pck_crl_size;
    void* p_root_ca_crl;
    uint32_t root_ca_crl_size;
    void* p_pck_crl_issuer_chain;
    uint32_t pck_crl_issuer_chain_size;
    void* p_qe_identity;
    uint32_t qe_identity_size;
    void* p_qe_identity_issuer_chain;
    uint32_t qe_identity_issuer_chain_size;
} oe_verify_quote_ocall_args_t;

typedef struct _oe_sgx_get_cpuid_table_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    void* cpuid_table_buffer;
    size_t cpuid_table_buffer_size;
} oe_sgx_get_cpuid_table_ocall_args_t;

typedef struct _oe_sgx_backtrace_symbols_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_enclave_t* oe_enclave;
    uint64_t* buffer;
    size_t size;
    void* symbols_buffer;
    size_t symbols_buffer_size;
    size_t* symbols_buffer_size_out;
} oe_sgx_backtrace_symbols_ocall_args_t;

typedef struct _oe_sgx_log_backtrace_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_enclave_t* oe_enclave;
    oe_log_level_t level;
    uint64_t* buffer;
    size_t size;
} oe_sgx_log_backtrace_ocall_args_t;

typedef struct _oe_sgx_thread_wake_wait_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_enclave_t* oe_enclave;
    uint64_t waiter_tcs;
    uint64_t self_tcs;
} oe_sgx_thread_wake_wait_ocall_args_t;

typedef struct _oe_sgx_wake_switchless_worker_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_host_worker_context_t* context;
} oe_sgx_wake_switchless_worker_ocall_args_t;

typedef struct _oe_sgx_sleep_switchless_worker_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_enclave_worker_context_t* context;
} oe_sgx_sleep_switchless_worker_ocall_args_t;

typedef struct _oe_get_tdx_quote_verification_collateral_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    void* p_quote;
    uint32_t quote_size;
    tdx_quote_collateral_t* collateral;
} oe_get_tdx_quote_verification_collateral_ocall_args_t;

typedef struct _oe_verify_tdx_quote_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    oe_result_t oe_retval;
    oe_uuid_t* format_id;
    void* opt_params;
    size_t opt_params_size;
    void* p_quote;
    uint32_t quote_size;
    void* p_endorsements;
    uint32_t endorsements_size;
    time_t expiration_check_date;
    uint32_t* p_collateral_expiration_status;
    uint32_t* p_quote_verification_result;
    void* p_qve_report_info;
    uint32_t qve_report_info_size;
    void* p_supplemental_data;
    uint32_t supplemental_data_size;
    uint32_t* p_supplemental_data_size_out;
} oe_verify_tdx_quote_ocall_args_t;

typedef struct _oe_log_is_supported_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
} oe_log_is_supported_ocall_args_t;

typedef struct _oe_log_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    uint32_t log_level;
    char* message;
    size_t message_len;
} oe_log_ocall_args_t;

typedef struct _oe_write_ocall_args_t
{
    oe_result_t oe_result;
    uint8_t* deepcopy_out_buffer;
    size_t deepcopy_out_buffer_size;
    int device;
    char* str;
    size_t str_len;
    size_t maxlen;
} oe_write_ocall_args_t;

/**** OCALL functions. ****/

static void ocall_ocall_onnx_load_model(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    ocall_onnx_load_model_args_t* _pargs_in = (ocall_onnx_load_model_args_t*)input_buffer;
    ocall_onnx_load_model_args_t* _pargs_out = (ocall_onnx_load_model_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->model_data)
        OE_SET_IN_POINTER(model_data, 1, _pargs_in->model_data_len, unsigned char*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->ocall_host_ret)
        OE_SET_OUT_POINTER(ocall_host_ret, 1, sizeof(oe_result_t), oe_result_t*);
    if (_pargs_in->host_return_value)
        OE_SET_OUT_POINTER(host_return_value, 1, sizeof(oe_result_t), oe_result_t*);
    if (_pargs_in->host_session_handle)
        OE_SET_OUT_POINTER(host_session_handle, 1, sizeof(uint64_t), uint64_t*);

    /* Call user function. */
    _pargs_out->oe_retval = ocall_onnx_load_model(
        _pargs_in->ocall_host_ret,
        _pargs_in->host_return_value,
        _pargs_in->host_session_handle,
        (const unsigned char*)_pargs_in->model_data,
        _pargs_in->model_data_len);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_ocall_onnx_run_inference(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    ocall_onnx_run_inference_args_t* _pargs_in = (ocall_onnx_run_inference_args_t*)input_buffer;
    ocall_onnx_run_inference_args_t* _pargs_out = (ocall_onnx_run_inference_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->input_data)
        OE_SET_IN_POINTER(input_data, 1, _pargs_in->input_len, void*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->ocall_host_ret)
        OE_SET_OUT_POINTER(ocall_host_ret, 1, sizeof(oe_result_t), oe_result_t*);
    if (_pargs_in->host_return_value)
        OE_SET_OUT_POINTER(host_return_value, 1, sizeof(oe_result_t), oe_result_t*);
    if (_pargs_in->output_data)
        OE_SET_OUT_POINTER(output_data, 1, _pargs_in->output_buf_len, void*);
    if (_pargs_in->actual_output_len)
        OE_SET_OUT_POINTER(actual_output_len, 1, sizeof(size_t), size_t*);

    /* Call user function. */
    _pargs_out->oe_retval = ocall_onnx_run_inference(
        _pargs_in->ocall_host_ret,
        _pargs_in->host_return_value,
        _pargs_in->host_session_handle,
        (const void*)_pargs_in->input_data,
        _pargs_in->input_len,
        _pargs_in->output_data,
        _pargs_in->output_buf_len,
        _pargs_in->actual_output_len);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_ocall_onnx_release_session(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    ocall_onnx_release_session_args_t* _pargs_in = (ocall_onnx_release_session_args_t*)input_buffer;
    ocall_onnx_release_session_args_t* _pargs_out = (ocall_onnx_release_session_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->ocall_host_ret)
        OE_SET_OUT_POINTER(ocall_host_ret, 1, sizeof(oe_result_t), oe_result_t*);
    if (_pargs_in->host_return_value)
        OE_SET_OUT_POINTER(host_return_value, 1, sizeof(oe_result_t), oe_result_t*);

    /* Call user function. */
    _pargs_out->oe_retval = ocall_onnx_release_session(
        _pargs_in->ocall_host_ret,
        _pargs_in->host_return_value,
        _pargs_in->host_session_handle);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_get_supported_attester_format_ids_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_get_supported_attester_format_ids_ocall_args_t* _pargs_in = (oe_get_supported_attester_format_ids_ocall_args_t*)input_buffer;
    oe_get_supported_attester_format_ids_ocall_args_t* _pargs_out = (oe_get_supported_attester_format_ids_ocall_args_t*)output_buffer;

    uint8_t* _deepcopy_out_buffer = NULL;
    size_t _deepcopy_out_buffer_offset = 0;
    size_t _deepcopy_out_buffer_size = 0;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->format_ids)
        OE_SET_OUT_POINTER(format_ids, 1, sizeof(format_ids_t), format_ids_t*);

    /* Call user function. */
    _pargs_out->oe_retval = oe_get_supported_attester_format_ids_ocall(
        _pargs_in->format_ids);

    /* Compute the size for the deep-copy out buffer. */
    if (_pargs_in->format_ids && _pargs_in->format_ids->data)
        OE_ADD_ARG_SIZE(_deepcopy_out_buffer_size, 1, _pargs_in->format_ids->size);

    if (_deepcopy_out_buffer_size)
    {
        _deepcopy_out_buffer = (uint8_t*) oe_malloc(_deepcopy_out_buffer_size);
        if (!_deepcopy_out_buffer)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }
    }

    /* Serialize the deep-copied content into the buffer. */
    if (_pargs_in->format_ids && _pargs_in->format_ids->data)
        OE_WRITE_DEEPCOPY_OUT_PARAM(_pargs_in->format_ids->data, 1, _pargs_in->format_ids->size);

    if (_deepcopy_out_buffer_offset != _deepcopy_out_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Set the _deepcopy_out_buffer and _deepcopy_out_buffer as part of _pargs_out. */
    _pargs_out->deepcopy_out_buffer = _deepcopy_out_buffer;
    _pargs_out->deepcopy_out_buffer_size = _deepcopy_out_buffer_size;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    /* Free _pargs_out->deepcopy_out_buffer on failure. */
    if (_result != OE_OK)
    {
        oe_free(_pargs_out->deepcopy_out_buffer);
        _pargs_out->deepcopy_out_buffer = NULL;
        _pargs_out->deepcopy_out_buffer_size = 0;
    }

    /* Free nested buffers allocated by the user function. */
    if (_pargs_in->format_ids)
    {
        for (size_t _i_1 = 0; _i_1 < 1; _i_1++)
        {
            free(_pargs_in->format_ids[_i_1].data);
        }
    }

    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_get_qetarget_info_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_get_qetarget_info_ocall_args_t* _pargs_in = (oe_get_qetarget_info_ocall_args_t*)input_buffer;
    oe_get_qetarget_info_ocall_args_t* _pargs_out = (oe_get_qetarget_info_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->format_id)
        OE_SET_IN_POINTER(format_id, 1, sizeof(oe_uuid_t), oe_uuid_t*);
    if (_pargs_in->opt_params)
        OE_SET_IN_POINTER(opt_params, 1, _pargs_in->opt_params_size, void*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->target_info)
        OE_SET_OUT_POINTER(target_info, 1, sizeof(sgx_target_info_t), sgx_target_info_t*);

    /* Call user function. */
    _pargs_out->oe_retval = oe_get_qetarget_info_ocall(
        (const oe_uuid_t*)_pargs_in->format_id,
        (const void*)_pargs_in->opt_params,
        _pargs_in->opt_params_size,
        _pargs_in->target_info);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_get_quote_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_get_quote_ocall_args_t* _pargs_in = (oe_get_quote_ocall_args_t*)input_buffer;
    oe_get_quote_ocall_args_t* _pargs_out = (oe_get_quote_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->format_id)
        OE_SET_IN_POINTER(format_id, 1, sizeof(oe_uuid_t), oe_uuid_t*);
    if (_pargs_in->opt_params)
        OE_SET_IN_POINTER(opt_params, 1, _pargs_in->opt_params_size, void*);
    if (_pargs_in->sgx_report)
        OE_SET_IN_POINTER(sgx_report, 1, sizeof(sgx_report_t), sgx_report_t*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->quote)
        OE_SET_OUT_POINTER(quote, 1, _pargs_in->quote_size, void*);
    if (_pargs_in->quote_size_out)
        OE_SET_OUT_POINTER(quote_size_out, 1, sizeof(size_t), size_t*);

    /* Call user function. */
    _pargs_out->oe_retval = oe_get_quote_ocall(
        (const oe_uuid_t*)_pargs_in->format_id,
        (const void*)_pargs_in->opt_params,
        _pargs_in->opt_params_size,
        (const sgx_report_t*)_pargs_in->sgx_report,
        _pargs_in->quote,
        _pargs_in->quote_size,
        _pargs_in->quote_size_out);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_get_quote_verification_collateral_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_get_quote_verification_collateral_ocall_args_t* _pargs_in = (oe_get_quote_verification_collateral_ocall_args_t*)input_buffer;
    oe_get_quote_verification_collateral_ocall_args_t* _pargs_out = (oe_get_quote_verification_collateral_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->fmspc)
        OE_SET_IN_POINTER(fmspc, 1, sizeof(uint8_t[6]), uint8_t*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->tcb_info)
        OE_SET_OUT_POINTER(tcb_info, 1, _pargs_in->tcb_info_size, void*);
    if (_pargs_in->tcb_info_size_out)
        OE_SET_OUT_POINTER(tcb_info_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->tcb_info_issuer_chain)
        OE_SET_OUT_POINTER(tcb_info_issuer_chain, 1, _pargs_in->tcb_info_issuer_chain_size, void*);
    if (_pargs_in->tcb_info_issuer_chain_size_out)
        OE_SET_OUT_POINTER(tcb_info_issuer_chain_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->pck_crl)
        OE_SET_OUT_POINTER(pck_crl, 1, _pargs_in->pck_crl_size, void*);
    if (_pargs_in->pck_crl_size_out)
        OE_SET_OUT_POINTER(pck_crl_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->root_ca_crl)
        OE_SET_OUT_POINTER(root_ca_crl, 1, _pargs_in->root_ca_crl_size, void*);
    if (_pargs_in->root_ca_crl_size_out)
        OE_SET_OUT_POINTER(root_ca_crl_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->pck_crl_issuer_chain)
        OE_SET_OUT_POINTER(pck_crl_issuer_chain, 1, _pargs_in->pck_crl_issuer_chain_size, void*);
    if (_pargs_in->pck_crl_issuer_chain_size_out)
        OE_SET_OUT_POINTER(pck_crl_issuer_chain_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->qe_identity)
        OE_SET_OUT_POINTER(qe_identity, 1, _pargs_in->qe_identity_size, void*);
    if (_pargs_in->qe_identity_size_out)
        OE_SET_OUT_POINTER(qe_identity_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->qe_identity_issuer_chain)
        OE_SET_OUT_POINTER(qe_identity_issuer_chain, 1, _pargs_in->qe_identity_issuer_chain_size, void*);
    if (_pargs_in->qe_identity_issuer_chain_size_out)
        OE_SET_OUT_POINTER(qe_identity_issuer_chain_size_out, 1, sizeof(size_t), size_t*);

    /* Call user function. */
    _pargs_out->oe_retval = oe_get_quote_verification_collateral_ocall(
        *(uint8_t(*)[6])_pargs_in->fmspc,
        _pargs_in->collateral_provider,
        _pargs_in->tcb_info,
        _pargs_in->tcb_info_size,
        _pargs_in->tcb_info_size_out,
        _pargs_in->tcb_info_issuer_chain,
        _pargs_in->tcb_info_issuer_chain_size,
        _pargs_in->tcb_info_issuer_chain_size_out,
        _pargs_in->pck_crl,
        _pargs_in->pck_crl_size,
        _pargs_in->pck_crl_size_out,
        _pargs_in->root_ca_crl,
        _pargs_in->root_ca_crl_size,
        _pargs_in->root_ca_crl_size_out,
        _pargs_in->pck_crl_issuer_chain,
        _pargs_in->pck_crl_issuer_chain_size,
        _pargs_in->pck_crl_issuer_chain_size_out,
        _pargs_in->qe_identity,
        _pargs_in->qe_identity_size,
        _pargs_in->qe_identity_size_out,
        _pargs_in->qe_identity_issuer_chain,
        _pargs_in->qe_identity_issuer_chain_size,
        _pargs_in->qe_identity_issuer_chain_size_out);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_get_quote_verification_collateral_with_baseline_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_get_quote_verification_collateral_with_baseline_ocall_args_t* _pargs_in = (oe_get_quote_verification_collateral_with_baseline_ocall_args_t*)input_buffer;
    oe_get_quote_verification_collateral_with_baseline_ocall_args_t* _pargs_out = (oe_get_quote_verification_collateral_with_baseline_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->fmspc)
        OE_SET_IN_POINTER(fmspc, 1, sizeof(uint8_t[6]), uint8_t*);
    if (_pargs_in->baseline)
        OE_SET_IN_POINTER(baseline, 1, _pargs_in->baseline_size, void*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->tcb_info)
        OE_SET_OUT_POINTER(tcb_info, 1, _pargs_in->tcb_info_size, void*);
    if (_pargs_in->tcb_info_size_out)
        OE_SET_OUT_POINTER(tcb_info_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->tcb_info_issuer_chain)
        OE_SET_OUT_POINTER(tcb_info_issuer_chain, 1, _pargs_in->tcb_info_issuer_chain_size, void*);
    if (_pargs_in->tcb_info_issuer_chain_size_out)
        OE_SET_OUT_POINTER(tcb_info_issuer_chain_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->pck_crl)
        OE_SET_OUT_POINTER(pck_crl, 1, _pargs_in->pck_crl_size, void*);
    if (_pargs_in->pck_crl_size_out)
        OE_SET_OUT_POINTER(pck_crl_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->root_ca_crl)
        OE_SET_OUT_POINTER(root_ca_crl, 1, _pargs_in->root_ca_crl_size, void*);
    if (_pargs_in->root_ca_crl_size_out)
        OE_SET_OUT_POINTER(root_ca_crl_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->pck_crl_issuer_chain)
        OE_SET_OUT_POINTER(pck_crl_issuer_chain, 1, _pargs_in->pck_crl_issuer_chain_size, void*);
    if (_pargs_in->pck_crl_issuer_chain_size_out)
        OE_SET_OUT_POINTER(pck_crl_issuer_chain_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->qe_identity)
        OE_SET_OUT_POINTER(qe_identity, 1, _pargs_in->qe_identity_size, void*);
    if (_pargs_in->qe_identity_size_out)
        OE_SET_OUT_POINTER(qe_identity_size_out, 1, sizeof(size_t), size_t*);
    if (_pargs_in->qe_identity_issuer_chain)
        OE_SET_OUT_POINTER(qe_identity_issuer_chain, 1, _pargs_in->qe_identity_issuer_chain_size, void*);
    if (_pargs_in->qe_identity_issuer_chain_size_out)
        OE_SET_OUT_POINTER(qe_identity_issuer_chain_size_out, 1, sizeof(size_t), size_t*);

    /* Call user function. */
    _pargs_out->oe_retval = oe_get_quote_verification_collateral_with_baseline_ocall(
        *(uint8_t(*)[6])_pargs_in->fmspc,
        _pargs_in->collateral_provider,
        _pargs_in->baseline,
        _pargs_in->baseline_size,
        _pargs_in->tcb_info,
        _pargs_in->tcb_info_size,
        _pargs_in->tcb_info_size_out,
        _pargs_in->tcb_info_issuer_chain,
        _pargs_in->tcb_info_issuer_chain_size,
        _pargs_in->tcb_info_issuer_chain_size_out,
        _pargs_in->pck_crl,
        _pargs_in->pck_crl_size,
        _pargs_in->pck_crl_size_out,
        _pargs_in->root_ca_crl,
        _pargs_in->root_ca_crl_size,
        _pargs_in->root_ca_crl_size_out,
        _pargs_in->pck_crl_issuer_chain,
        _pargs_in->pck_crl_issuer_chain_size,
        _pargs_in->pck_crl_issuer_chain_size_out,
        _pargs_in->qe_identity,
        _pargs_in->qe_identity_size,
        _pargs_in->qe_identity_size_out,
        _pargs_in->qe_identity_issuer_chain,
        _pargs_in->qe_identity_issuer_chain_size,
        _pargs_in->qe_identity_issuer_chain_size_out);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_verify_quote_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_verify_quote_ocall_args_t* _pargs_in = (oe_verify_quote_ocall_args_t*)input_buffer;
    oe_verify_quote_ocall_args_t* _pargs_out = (oe_verify_quote_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->format_id)
        OE_SET_IN_POINTER(format_id, 1, sizeof(oe_uuid_t), oe_uuid_t*);
    if (_pargs_in->opt_params)
        OE_SET_IN_POINTER(opt_params, 1, _pargs_in->opt_params_size, void*);
    if (_pargs_in->p_quote)
        OE_SET_IN_POINTER(p_quote, 1, _pargs_in->quote_size, void*);
    if (_pargs_in->p_qve_report_info)
        OE_SET_IN_OUT_POINTER(p_qve_report_info, 1, _pargs_in->qve_report_info_size, void*);
    if (_pargs_in->p_tcb_info)
        OE_SET_IN_POINTER(p_tcb_info, 1, _pargs_in->tcb_info_size, void*);
    if (_pargs_in->p_tcb_info_issuer_chain)
        OE_SET_IN_POINTER(p_tcb_info_issuer_chain, 1, _pargs_in->tcb_info_issuer_chain_size, void*);
    if (_pargs_in->p_pck_crl)
        OE_SET_IN_POINTER(p_pck_crl, 1, _pargs_in->pck_crl_size, void*);
    if (_pargs_in->p_root_ca_crl)
        OE_SET_IN_POINTER(p_root_ca_crl, 1, _pargs_in->root_ca_crl_size, void*);
    if (_pargs_in->p_pck_crl_issuer_chain)
        OE_SET_IN_POINTER(p_pck_crl_issuer_chain, 1, _pargs_in->pck_crl_issuer_chain_size, void*);
    if (_pargs_in->p_qe_identity)
        OE_SET_IN_POINTER(p_qe_identity, 1, _pargs_in->qe_identity_size, void*);
    if (_pargs_in->p_qe_identity_issuer_chain)
        OE_SET_IN_POINTER(p_qe_identity_issuer_chain, 1, _pargs_in->qe_identity_issuer_chain_size, void*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->p_collateral_expiration_status)
        OE_SET_OUT_POINTER(p_collateral_expiration_status, 1, sizeof(uint32_t), uint32_t*);
    if (_pargs_in->p_quote_verification_result)
        OE_SET_OUT_POINTER(p_quote_verification_result, 1, sizeof(uint32_t), uint32_t*);
    if (_pargs_in->p_qve_report_info)
        OE_COPY_AND_SET_IN_OUT_POINTER(p_qve_report_info, 1, _pargs_in->qve_report_info_size, void*);
    if (_pargs_in->p_supplemental_data)
        OE_SET_OUT_POINTER(p_supplemental_data, 1, _pargs_in->supplemental_data_size, void*);
    if (_pargs_in->p_supplemental_data_size_out)
        OE_SET_OUT_POINTER(p_supplemental_data_size_out, 1, sizeof(uint32_t), uint32_t*);

    /* Call user function. */
    _pargs_out->oe_retval = oe_verify_quote_ocall(
        (const oe_uuid_t*)_pargs_in->format_id,
        (const void*)_pargs_in->opt_params,
        _pargs_in->opt_params_size,
        (const void*)_pargs_in->p_quote,
        _pargs_in->quote_size,
        _pargs_in->expiration_check_date,
        _pargs_in->p_collateral_expiration_status,
        _pargs_in->p_quote_verification_result,
        _pargs_in->p_qve_report_info,
        _pargs_in->qve_report_info_size,
        _pargs_in->p_supplemental_data,
        _pargs_in->supplemental_data_size,
        _pargs_in->p_supplemental_data_size_out,
        _pargs_in->collateral_version,
        (const void*)_pargs_in->p_tcb_info,
        _pargs_in->tcb_info_size,
        (const void*)_pargs_in->p_tcb_info_issuer_chain,
        _pargs_in->tcb_info_issuer_chain_size,
        (const void*)_pargs_in->p_pck_crl,
        _pargs_in->pck_crl_size,
        (const void*)_pargs_in->p_root_ca_crl,
        _pargs_in->root_ca_crl_size,
        (const void*)_pargs_in->p_pck_crl_issuer_chain,
        _pargs_in->pck_crl_issuer_chain_size,
        (const void*)_pargs_in->p_qe_identity,
        _pargs_in->qe_identity_size,
        (const void*)_pargs_in->p_qe_identity_issuer_chain,
        _pargs_in->qe_identity_issuer_chain_size);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_sgx_get_cpuid_table_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_sgx_get_cpuid_table_ocall_args_t* _pargs_in = (oe_sgx_get_cpuid_table_ocall_args_t*)input_buffer;
    oe_sgx_get_cpuid_table_ocall_args_t* _pargs_out = (oe_sgx_get_cpuid_table_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->cpuid_table_buffer)
        OE_SET_OUT_POINTER(cpuid_table_buffer, 1, _pargs_in->cpuid_table_buffer_size, void*);

    /* Call user function. */
    _pargs_out->oe_retval = oe_sgx_get_cpuid_table_ocall(
        _pargs_in->cpuid_table_buffer,
        _pargs_in->cpuid_table_buffer_size);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_sgx_backtrace_symbols_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_sgx_backtrace_symbols_ocall_args_t* _pargs_in = (oe_sgx_backtrace_symbols_ocall_args_t*)input_buffer;
    oe_sgx_backtrace_symbols_ocall_args_t* _pargs_out = (oe_sgx_backtrace_symbols_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->buffer)
        OE_SET_IN_POINTER(buffer, _pargs_in->size, sizeof(uint64_t), uint64_t*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->symbols_buffer)
        OE_SET_OUT_POINTER(symbols_buffer, 1, _pargs_in->symbols_buffer_size, void*);
    if (_pargs_in->symbols_buffer_size_out)
        OE_SET_OUT_POINTER(symbols_buffer_size_out, 1, sizeof(size_t), size_t*);

    /* Call user function. */
    _pargs_out->oe_retval = oe_sgx_backtrace_symbols_ocall(
        _pargs_in->oe_enclave,
        (const uint64_t*)_pargs_in->buffer,
        _pargs_in->size,
        _pargs_in->symbols_buffer,
        _pargs_in->symbols_buffer_size,
        _pargs_in->symbols_buffer_size_out);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_sgx_log_backtrace_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_sgx_log_backtrace_ocall_args_t* _pargs_in = (oe_sgx_log_backtrace_ocall_args_t*)input_buffer;
    oe_sgx_log_backtrace_ocall_args_t* _pargs_out = (oe_sgx_log_backtrace_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->buffer)
        OE_SET_IN_POINTER(buffer, _pargs_in->size, sizeof(uint64_t), uint64_t*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Call user function. */
    _pargs_out->oe_retval = oe_sgx_log_backtrace_ocall(
        _pargs_in->oe_enclave,
        _pargs_in->level,
        (const uint64_t*)_pargs_in->buffer,
        _pargs_in->size);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_sgx_thread_wake_wait_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_sgx_thread_wake_wait_ocall_args_t* _pargs_in = (oe_sgx_thread_wake_wait_ocall_args_t*)input_buffer;
    oe_sgx_thread_wake_wait_ocall_args_t* _pargs_out = (oe_sgx_thread_wake_wait_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Call user function. */
    oe_sgx_thread_wake_wait_ocall(
        _pargs_in->oe_enclave,
        _pargs_in->waiter_tcs,
        _pargs_in->self_tcs);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_sgx_wake_switchless_worker_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_sgx_wake_switchless_worker_ocall_args_t* _pargs_in = (oe_sgx_wake_switchless_worker_ocall_args_t*)input_buffer;
    oe_sgx_wake_switchless_worker_ocall_args_t* _pargs_out = (oe_sgx_wake_switchless_worker_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Call user function. */
    oe_sgx_wake_switchless_worker_ocall(
        _pargs_in->context);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_sgx_sleep_switchless_worker_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_sgx_sleep_switchless_worker_ocall_args_t* _pargs_in = (oe_sgx_sleep_switchless_worker_ocall_args_t*)input_buffer;
    oe_sgx_sleep_switchless_worker_ocall_args_t* _pargs_out = (oe_sgx_sleep_switchless_worker_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Call user function. */
    oe_sgx_sleep_switchless_worker_ocall(
        _pargs_in->context);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_get_tdx_quote_verification_collateral_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_get_tdx_quote_verification_collateral_ocall_args_t* _pargs_in = (oe_get_tdx_quote_verification_collateral_ocall_args_t*)input_buffer;
    oe_get_tdx_quote_verification_collateral_ocall_args_t* _pargs_out = (oe_get_tdx_quote_verification_collateral_ocall_args_t*)output_buffer;

    uint8_t* _deepcopy_out_buffer = NULL;
    size_t _deepcopy_out_buffer_offset = 0;
    size_t _deepcopy_out_buffer_size = 0;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->p_quote)
        OE_SET_IN_POINTER(p_quote, 1, _pargs_in->quote_size, void*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->collateral)
        OE_SET_OUT_POINTER(collateral, 1, sizeof(tdx_quote_collateral_t), tdx_quote_collateral_t*);

    /* Call user function. */
    _pargs_out->oe_retval = oe_get_tdx_quote_verification_collateral_ocall(
        (const void*)_pargs_in->p_quote,
        _pargs_in->quote_size,
        _pargs_in->collateral);

    /* Compute the size for the deep-copy out buffer. */
    if (_pargs_in->collateral && _pargs_in->collateral->data)
        OE_ADD_ARG_SIZE(_deepcopy_out_buffer_size, 1, _pargs_in->collateral->size);

    if (_deepcopy_out_buffer_size)
    {
        _deepcopy_out_buffer = (uint8_t*) oe_malloc(_deepcopy_out_buffer_size);
        if (!_deepcopy_out_buffer)
        {
            _result = OE_OUT_OF_MEMORY;
            goto done;
        }
    }

    /* Serialize the deep-copied content into the buffer. */
    if (_pargs_in->collateral && _pargs_in->collateral->data)
        OE_WRITE_DEEPCOPY_OUT_PARAM(_pargs_in->collateral->data, 1, _pargs_in->collateral->size);

    if (_deepcopy_out_buffer_offset != _deepcopy_out_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }

    /* Set the _deepcopy_out_buffer and _deepcopy_out_buffer as part of _pargs_out. */
    _pargs_out->deepcopy_out_buffer = _deepcopy_out_buffer;
    _pargs_out->deepcopy_out_buffer_size = _deepcopy_out_buffer_size;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    /* Free _pargs_out->deepcopy_out_buffer on failure. */
    if (_result != OE_OK)
    {
        oe_free(_pargs_out->deepcopy_out_buffer);
        _pargs_out->deepcopy_out_buffer = NULL;
        _pargs_out->deepcopy_out_buffer_size = 0;
    }

    /* Free nested buffers allocated by the user function. */
    if (_pargs_in->collateral)
    {
        for (size_t _i_1 = 0; _i_1 < 1; _i_1++)
        {
            free(_pargs_in->collateral[_i_1].data);
        }
    }

    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_verify_tdx_quote_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_verify_tdx_quote_ocall_args_t* _pargs_in = (oe_verify_tdx_quote_ocall_args_t*)input_buffer;
    oe_verify_tdx_quote_ocall_args_t* _pargs_out = (oe_verify_tdx_quote_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->format_id)
        OE_SET_IN_POINTER(format_id, 1, sizeof(oe_uuid_t), oe_uuid_t*);
    if (_pargs_in->opt_params)
        OE_SET_IN_POINTER(opt_params, 1, _pargs_in->opt_params_size, void*);
    if (_pargs_in->p_quote)
        OE_SET_IN_POINTER(p_quote, 1, _pargs_in->quote_size, void*);
    if (_pargs_in->p_endorsements)
        OE_SET_IN_POINTER(p_endorsements, 1, _pargs_in->endorsements_size, void*);
    if (_pargs_in->p_qve_report_info)
        OE_SET_IN_OUT_POINTER(p_qve_report_info, 1, _pargs_in->qve_report_info_size, void*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (_pargs_in->p_collateral_expiration_status)
        OE_SET_OUT_POINTER(p_collateral_expiration_status, 1, sizeof(uint32_t), uint32_t*);
    if (_pargs_in->p_quote_verification_result)
        OE_SET_OUT_POINTER(p_quote_verification_result, 1, sizeof(uint32_t), uint32_t*);
    if (_pargs_in->p_qve_report_info)
        OE_COPY_AND_SET_IN_OUT_POINTER(p_qve_report_info, 1, _pargs_in->qve_report_info_size, void*);
    if (_pargs_in->p_supplemental_data)
        OE_SET_OUT_POINTER(p_supplemental_data, 1, _pargs_in->supplemental_data_size, void*);
    if (_pargs_in->p_supplemental_data_size_out)
        OE_SET_OUT_POINTER(p_supplemental_data_size_out, 1, sizeof(uint32_t), uint32_t*);

    /* Call user function. */
    _pargs_out->oe_retval = oe_verify_tdx_quote_ocall(
        (const oe_uuid_t*)_pargs_in->format_id,
        (const void*)_pargs_in->opt_params,
        _pargs_in->opt_params_size,
        (const void*)_pargs_in->p_quote,
        _pargs_in->quote_size,
        (const void*)_pargs_in->p_endorsements,
        _pargs_in->endorsements_size,
        _pargs_in->expiration_check_date,
        _pargs_in->p_collateral_expiration_status,
        _pargs_in->p_quote_verification_result,
        _pargs_in->p_qve_report_info,
        _pargs_in->qve_report_info_size,
        _pargs_in->p_supplemental_data,
        _pargs_in->supplemental_data_size,
        _pargs_in->p_supplemental_data_size_out);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_log_is_supported_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_log_is_supported_ocall_args_t* _pargs_in = (oe_log_is_supported_ocall_args_t*)input_buffer;
    oe_log_is_supported_ocall_args_t* _pargs_out = (oe_log_is_supported_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Call user function. */
    oe_log_is_supported_ocall(
    );

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_log_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_log_ocall_args_t* _pargs_in = (oe_log_ocall_args_t*)input_buffer;
    oe_log_ocall_args_t* _pargs_out = (oe_log_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->message)
        OE_SET_IN_POINTER(message, _pargs_in->message_len, sizeof(char), char*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Call user function. */
    oe_log_ocall(
        _pargs_in->log_level,
        (const char*)_pargs_in->message);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

static void ocall_oe_write_ocall(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;
    OE_UNUSED(input_buffer_size);

    /* Prepare parameters. */
    oe_write_ocall_args_t* _pargs_in = (oe_write_ocall_args_t*)input_buffer;
    oe_write_ocall_args_t* _pargs_out = (oe_write_ocall_args_t*)output_buffer;

    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));

    if (input_buffer_size < sizeof(*_pargs_in) || output_buffer_size < sizeof(*_pargs_in))
        goto done;

    /* Make sure input and output buffers are valid. */
    if (!input_buffer || !output_buffer) {
        _result = OE_INVALID_PARAMETER;
        goto done;
    }

    /* Set in and in-out pointers. */
    if (_pargs_in->str)
        OE_SET_IN_POINTER(str, _pargs_in->str_len, sizeof(char), char*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Call user function. */
    oe_write_ocall(
        _pargs_in->device,
        (const char*)_pargs_in->str,
        _pargs_in->maxlen);

    /* There is no deep-copyable out parameter. */
    _pargs_out->deepcopy_out_buffer = NULL;
    _pargs_out->deepcopy_out_buffer_size = 0;

    /* Propagate errno back to enclave. */
    /* Errno propagation not enabled. */

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = _output_buffer_offset;

done:
    if (_pargs_out && output_buffer_size >= sizeof(*_pargs_out))
        _pargs_out->oe_result = _result;
}

/**** OCALL function table. ****/

static oe_ocall_func_t _enclave_ocall_function_table[] = {
    (oe_ocall_func_t) ocall_ocall_onnx_load_model,
    (oe_ocall_func_t) ocall_ocall_onnx_run_inference,
    (oe_ocall_func_t) ocall_ocall_onnx_release_session,
    (oe_ocall_func_t) ocall_oe_get_supported_attester_format_ids_ocall,
    (oe_ocall_func_t) ocall_oe_get_qetarget_info_ocall,
    (oe_ocall_func_t) ocall_oe_get_quote_ocall,
    (oe_ocall_func_t) ocall_oe_get_quote_verification_collateral_ocall,
    (oe_ocall_func_t) ocall_oe_get_quote_verification_collateral_with_baseline_ocall,
    (oe_ocall_func_t) ocall_oe_verify_quote_ocall,
    (oe_ocall_func_t) ocall_oe_sgx_get_cpuid_table_ocall,
    (oe_ocall_func_t) ocall_oe_sgx_backtrace_symbols_ocall,
    (oe_ocall_func_t) ocall_oe_sgx_log_backtrace_ocall,
    (oe_ocall_func_t) ocall_oe_sgx_thread_wake_wait_ocall,
    (oe_ocall_func_t) ocall_oe_sgx_wake_switchless_worker_ocall,
    (oe_ocall_func_t) ocall_oe_sgx_sleep_switchless_worker_ocall,
    (oe_ocall_func_t) ocall_oe_get_tdx_quote_verification_collateral_ocall,
    (oe_ocall_func_t) ocall_oe_verify_tdx_quote_ocall,
    (oe_ocall_func_t) ocall_oe_log_is_supported_ocall,
    (oe_ocall_func_t) ocall_oe_log_ocall,
    (oe_ocall_func_t) ocall_oe_write_ocall,
    NULL
};

oe_result_t oe_create_enclave_enclave(
    const char* path,
    oe_enclave_type_t type,
    uint32_t flags,
    const oe_enclave_setting_t* settings,
    uint32_t setting_count,
    oe_enclave_t** enclave)
{
    return oe_create_enclave(
               path,
               type,
               flags,
               settings,
               setting_count,
               _enclave_ocall_function_table,
               20,
               _enclave_ecall_info_table,
                9,
               enclave);
}

OE_EXTERNC_END
